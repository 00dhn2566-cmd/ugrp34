# 세션 상황판 (SESSIONS_BOARD)

여러 클로드 세션이 병행 작업할 때의 **단일 보고 창구** (사용자 요청 2026-07-16:
"이런 보고체계 있어가지고 쭉 이어서 진행할 수 있었으면 좋겠다").

## 규칙
- 각 세션은 **자기 섹션만** 갱신 (한 줄 = 사건 1건, 최신이 위, `날짜 시각 — 내용 (커밋)`)
- 다른 세션에 넘길 것/받을 것은 ★로 표시 — 상대 세션은 소비 후 ★를 지우고 자기 줄로 기록
- 상세는 각자 문서(TUNING_STATUS/PIPELINE_STATUS/README)에, 여기는 헤드라인만
- MATLAB 사용 전 여기서 점유 여부 확인 후 자기 줄로 선언 (1대 규칙, RAM 16GB)

## MATLAB 점유
- **08-23 지연 강건화 세션 점유 중** — `sweep_delay_spec`(tau x 배율, 1 kg 완료 / 0 kg 대기). 끝나면 해제 기록할 것
- (비어 있음) — 08-18 동역학 세션 3편(`probe_prop_and_mixer` / `probe_2kg_law` / 앞선 저질량 2편) 완료·해제. 지니 노트북: 08-18 19:05 성능 지표 세션(스케줄 배터리 + 0 kg 미션 매트릭스) 완료·해제.

## 튜닝/C++ 세션 (17차 계열)
- 08-18 저녁 — **골든 트레이스 대조 완료 → 믹서 부호·모멘트 암 정정** (`compare_golden.py` + `diagnose/golden_plant_export*.m`) — 각가속도 직접 상관으로 재검정. **아래 줄 ②의 부호표는 틀렸다**: roll `[-1,+1,-1,+1]` / pitch `[+1,+1,-1,-1]` (**둘 다 반전**) / yaw `[-1,+1,+1,-1]`(불변) — 피치 상관이 −0.916 → **+0.975**로 뒤집혀 확정. 오류 원인은 **정착 각도로 부호를 유추한 것** (각도 유지 구간은 순토크≈0이라 각도-차동량 관계로 토크 부호를 읽을 수 없음. 옳은 검정은 각가속도와의 순간 상관). ③의 "균등 가중치"도 정정 — **유효 모멘트 암 ℓ = 0.0930 m** (가정했던 X-쿼드 균등값 0.159 m가 아님; roll/pitch 유효계수 0.0932 vs 0.0929로 0.4% 이내 일치 = 플랜트 대칭 자기검증). **개별 모터 암은 식별 불가** — 조건수 1.1e4, 4파라미터 R² 0.8404가 1파라미터 0.839와 무차이(호버에서 네 추력이 거의 같아 분리 안 됨). 개루프 모터 가진 필요 (§11.3). 병진도 검증 통과(x 진폭 0.4% 일치, 호버 z 잔차 평균 +0.00136 m/s²). 방법 전환 기록: n(t) 재생은 **실패**(감쇠 없는 이중적분기라 추력 불균형 0.08 N만으로 수 초 만에 발산, 위치 오차 16 m) → 잔차 대조로 교체. 함정 실증: Simscape rpm에 물리 Ct를 먹여 위치 오차 35 m — §5.3 경고가 실제로 사람을 잡음. 문서 반영 `docs/DYNAMICS.md` §10.2/§10.4/§11.1/§11.3. **★튜닝/C++ 세션**: `qc_controller.hpp:155`의 `[TODO-verify: 입력 포트 순서 — 골든 트레이스가 최종 판정]`이 이제 판정 가능. 단 C++ 표(pitch `[+1,-1,-1,+1]`/roll `[-1,-1,+1,+1]`/yaw `[-1,+1,-1,+1]`, 블록 덤프 출처)와 위 실측표가 **모터 인덱스 치환만큼 어긋나** 보임 — 로터 인덱스→기하 매핑(윤호 seam에서도 미정) 확정과 함께 대조 요망
- 08-18 — **자세 게인 스케일 법칙: 판별 불가로 종결** (`probe_2kg_law.m`) — 2kg에서 후보 3종(1차식 1.250 / I·m^-0.5 1.077 / 관성비 1.293) 대조: 추종 3.96 / 4.04 / 4.01, 오버 7.6 / 7.8 / 7.7 — **게인 20% 차이에 성능 2% 변동**으로 구별 불가. A행은 18차 기록 소수점까지 재현(회귀 무결). ★핵심: 실패 원인이 0kg과 **정반대** — 0kg은 과민(3e-7에 34% 변동), 2kg은 둔감(20%에 2% 변동). 즉 **법칙의 형태가 결과를 좌우하는 구간은 저질량뿐이고 그 구간은 측정 불가**. 0.5~2kg은 어떤 법칙이든 무방하며 중요한 건 보정의 존재 자체. → 현행 1차식 유지, 보고서엔 물리 유도가 아니라 **경험적 보정**으로 기술할 것 (DYNAMICS.md §11.2)
- 08-18 — **★미확정 2건 실측 확정 (믹서 부호표 / 프로펠러 회전속도)** — `diagnose/probe_prop_and_mixer.m` 신설·실행. ① **프로펠러 단위 결착**: 호버 `Prop.w` 실측 633.7(평균 크기), 추력 5.55 N/모터(합 22.27 N, 필요 22.29 N과 0.06% 일치). 블록이 kT=9.79를 쓰므로 역산 n=10.56 rev/s → **로그 채널은 rpm이고 프로펠러는 66.4 rad/s로 돈다**. 즉 TUNING_STATUS/`qc_motor.hpp`의 "평형 634 **rad/s**"는 실제로 **634 rpm**이며, 91.3배는 (30/π)² 단위 불일치를 kT가 흡수한 것. C++ 모델(Ct=0.1072 @ 634 rad/s)과 Simscape(kT=9.79 @ 634 rpm)는 **회전속도가 다르지만 추력은 같다** — Gazebo 이식 시 주의. ② ~~**믹서 부호표 확정**: roll `[+1,-1,+1,-1]` / pitch `[-1,-1,+1,+1]` / yaw `[-1,+1,+1,-1]`.~~ **[정정 08-18 저녁 — roll·pitch 반전, 윗줄 참조]** 세 행 + 추력 행이 서로 모두 직교(그람=4I)로 자기검증. 회전방향 `[+,-,-,+]`(M2·M3 역회전) 실측. ③ **신규 발견**: CAD 암 배치가 90° 간격이되 축에서 −11.7° 회전한 "+"형 — 45° X-쿼드 아님. 관성은 무영향(반지름만 관여, qc_phys 유효), 모멘트 암 가중치만 영향. 단 기하 가중치는 교차결합 66%를 예측하는데 실측은 16%라 **반증** → ~~균등 가중치가 더 나은 근사~~ **[정정 08-18 저녁 — 유효 ℓ=0.0930 m, 개별 암은 식별 불가]**, 정밀 확정은 모터 좌표 실측 필요. ④ **독립 6-DOF 적분기 신설** (`control_seoungjin/plant_sim.py`) — 골든 트레이스의 파이썬 절반. 자체검증 3/3(호버 평형 고도드리프트 0.000cm, 자유낙하 항력 +30cm, 스핀다운 1/e 0.069s). n(t) 규정 모드·CSV 재생 포함. ⑤ 문서: `docs/DYNAMICS.md` §10.2 부호 교체·§11 2건 종결·§11.0 신설, `docs/DYNAMICS_INTRO.md` 신설(입문용)
- 08-18 — **동역학 문서화 착수(지도교수 요구)** + 저질량 실험 2편. `.slx` 전량 추출(위상/질량·관성/프로펠러/항력/모터/접촉) → **`docs/HANDOFF_DYNAMICS.md` 신설**(성능 좋은 머신 세션용, 재료 전량 수록). 실측 ① **0kg precision 3구성**: 고정게인은 오버슈트 4293cm 완전 붕괴 vs 질량보정 37cm 생존 — 단 **스펙 4항 중 3항 탈락**(오버 37.1 / 추종 21.4 / z 11.0 cm, 기준 각 ≤10) → "저질량 precision 권장"은 스펙 만족이 아니라 **생존**의 뜻. **혼돈 정량화**: 게인 상대 3e-7 섭동에 추종 21.35→28.61cm(34%) 변동, 시뮬 자체는 비트단위 결정론적 → 0kg 게인 미세비교는 원리적으로 무의미. ② **기체 증량 @0kg 실패** — 고도 트림(`Bias Chassis`)·게인(`sZ`)이 `m_pkg`만 보고 기체 질량을 몰라 +250/500g 모두 못 뜸(z피크 85cm), 트림 보정도 실패. ★미해결: "관성 키우면 자세 나아지나"는 고도 붕괴에 가려 미답 — `qc_phys(drone_mass+dm,0,·)`로 게인·트림 동시 재계산 필요. 부수 발견: `drone_mass`는 `.slx`가 미참조(게인 계산 전용), 실제 기체질량 knob은 `Body/Body/Flight Computer`의 `Mass`
- 07-19 밤 — **비상 대비책 구상(사용자): 자작 제어기 계속 진행, 의도대로 안 되면 PX4 전환** — 전환 트리거 후보: ① 실기 이식에서 시뮬 대비 열화가 재튜닝으로 안 잡힘(자세 kd는 측정 지연 0.05s 기준 — 실기 지연 다르면 재튜닝) ② 페일세이프 개발 비용 폭발 ③ 실비행 데드라인. 보험: 궤적 산출물이 컨트롤러 중립이라 "궤적→MAVLink offboard" 어댑터 하나로 상위 전체 이식 가능
- 07-19 밤 — **램프·지터 대조 완결** (882030e): 등속 추종 지연 precision ±2cm/agile ±0.6cm (0.5~2.0m/s 전 속도), 정지 오버 3.8~6.4 vs 0.4~1.8cm. **잔류 지터 대조 증명**: 깨끗한 기준에서 precision ±0.000도(호버급 복귀)/agile ±0.27도(r8 본성) — 스텝 시험의 ±0.55도 지속 진동은 성형기 꼬리 몫으로 확정 (★path_time 스무더 건 보강 증거)
- 07-19 밤 — **전체 파이프라인 스텝응답 실측** (8add65e, 사용자 요청): rise 10-90% 0.57~0.59s(0.1m)/0.89~0.95s(1m·성형 지배), 도착 오버슈트 precision +4.8cm vs agile +0.1cm, 제어기 SSE=meas−ref 수 mm, 외란 복귀 0.44s. ★path_time: **traj_smoother에 준-스텝(0.1s 램프) 직접 입력 시 꼬리 저주파 진동 발견** (기준 궤적이 ±2cm/~0.2Hz로 출렁, 제어기는 mm급 추종 — 시계열 step_ts_*.csv). 실사용 경로(게이트 거부→재시간화 선행)는 안 타는 경로로 보이나 스무더 단독 오용 가드 검토 요망
- 07-19 — **agile 최종 채택: 삼각 법칙 + z분리** (9026688) — kp_xy=24-16|m-1| (양끝 precision 수렴), z는 8/3.2 고정(벡터 게인+posErrSatZ). 삼각 경사 1.5/1.75/2kg 합격(1.91~3.96cm, z꼬리<1cm — 2kg 한계사이클 소멸), 최종 관문(채택 경로) 외란 1.55도/0.44s + 1kg 이동 1.28cm 합격. C++ qc_apply_profile 동기(precision 골든 비트 동일). MATLAB 해제. ★전 세션: `run_traj_baked.m`에 `qc_zsplit_apply(mdl)` 호출 추가됨 — 직접 load_system 하는 스크립트로 agile 돌릴 땐 이 헬퍼 호출 필수 (비-agile은 무해·선택). ★path_time/RL: agile 유효구간 0.5~2kg, 0.5kg 미만은 precision 권장 (혼돈 구간 실측)
- 07-19 — smoother 백포팅 재검증 **합격** (diagnose_smoother: 발산 궤적 적발/정상 무개입 0.0000m/성형 후 안정 비행 |x| 1.08m·자세 13.2도·모터 81% 무포화) — path_time ★큐 소비 완료
- 07-19 — agile 1차식 A/B/C 실측 (8734bf1): A(외삽) 1.5kg부터 발산 — 위치 절벽 33 불변. B(1kg캡) 수평 전 질량 해결, z피크 잔존. C(z분리, 사용자 "z가 문제") 이동 중 z 해결(42→1.3cm), 2kg 85cm는 **정착 후 한계사이클**로 판명. 0~0.25kg는 혼돈 구간(재현성 없음). 삼각 법칙(kp_xy=24-16|m-1|, 2kg=precision 수렴) 경사 검증 중
- 07-19 — **agile 0kg 격자 전멸 (9/9)** — 위치 24/10.8 고정 시 sA/sZ 어떤 조합도 0kg 생존 불가 (최선 54cm/오버 483cm) → **범인 = 위치 kp 자체** (z축 결합: 위치 kp가 z 위치 오차에도 걸림). 사용자 확정 "위치 PID도 똑같이 1차식" — kp(m)=8+16m, kd(m)=3.2+7.6m (0kg=precision 앵커와 일치=실측 생존점, 1kg=agile 24/10.8). A(2kg 외삽 kp40)/B(위치 1kg 캡) 8구성 검증 실행 중
- 07-19 — **agile 관문 판정: 외란 합격 / 질량 불합격** — 외란 펄스 이탈 1.65도·회복 0.44s(여유 큼), 1kg 이동 1.32cm(r6 재현). 그러나 **0.5kg 완전 발산**(추종 193cm/자세 48도) + 2kg z피크 85cm(위치 kp가 z축에도 걸려 고도 과출력). agile은 1kg 전용임이 실증 — 처분(제거/1kg 제한/agile-lite 교체)은 사용자 결정 대기 (verify_agile_gates.csv)
- 07-19 — **질량 1차식 게인 법칙 채택 완료** (사용자 지시 이행): 0kg 앵커 sA=0.75/sZ=0.56 (r2 재현 확인) → 6질량 검증 합격(0~2kg 무발산, 1kg 회귀 무결 4.08cm, 0.5 내삽 비열등, 2kg 외삽 우세) → parameters.m `sA_mass/sZ_mass` 반영 + C++ `qc_scales` 동기(1kg 골든 재생 비트 동일). yaw는 질량 동결. 약점: 0~0.25kg 전이 20~29cm(안정 유지). 상세 TUNING_STATUS §Y 18차
- 07-19 — **0kg 앵커 r1 완료** (9점 격자): 승자 sA=0.75/sZ=0.56 (추종 21.4cm/오버 37cm, 유일 무발산). 예측 뒤집힘 — 자세도 25% 감쇠가 최선 (동결 sA=1.0은 오버 759~4294cm 발산). sZ 하단 모서리라 r2 (sA 0.65~0.85 × sZ 0.40~0.56) 실행 중 (~14분). 이후: 1차식 6점 검증 → parameters.m 반영
- 07-19 — 질량 탐침 완료: 붕괴 경계 0.3~0.1kg. xy 붕괴=자세 게인 과소(sIa≤0.61), z 붕괴=고도 게인 과대(sM=1.0, z피크 83~84cm) — 채널 분리 확인, 0kg 앵커 설계 근거 (refine_mass_probe.csv)
- 07-19 — 비상 세션용: HANDOFF_EMERGENCY **§8 추가** (제어기 내부 실측 — 클램프 지도/여유 추력 구조/측정 지연 0.05s/anti-windup 부재/한계사이클/플랜트 진실 주입 도구/C++ 협의 규칙). B·C 반사 설계 전 필독 (577c884) [★소비됨: 비상 세션 정독 완료 07-19]
- 07-18 밤 — **골든 대조 1차 합격**: cmd_pitch/roll RMS 0.07%/0.00%, corr 0.9999 — C++ 위치 체인 = Simulink 동일성 증명. 덤프 확정 3건 반영(믹서 표/측정필터 0.05/고도 2단 클램프). **질량 1차식 게인 법칙 명세 확정(사용자)** — 선행: 0kg 앵커 (ad4b855)
- 07-17 01:45 — **⚠ 전 세션 공유: 0kg(생 드론) 레짐 붕괴 실측** — 정규화 ON/OFF 무관 준발산(오버슈트 2m/43m, ON이 그나마 방어). 붕괴는 0.5~0kg 사이(0.5kg은 정상) — 탐침으로 국소화 예정 (validate_phys_ab0.csv). [정정 02:10: path_time 발견 "임무에 투하 없음(사용자 확인)"에 따라 "복귀 구간 미지원" 해석 철회 — 0kg은 운영 구간이 아니라 **과적합 경계** 이슈. 단 생 드론 시운전 시 주의는 유효]
- 07-17 01:10 — **위치 게인 결정(사용자): 프로파일 3종 + 상위 선택** — precision(8/3.2, 기본)/balanced(12/4.8)/agile(24/10.8), 임무 단위 전환(v1). parameters.m `ctrl_profile` switch + C++ `qc_apply_profile` 동기 구현·검증. ★path_time 세션: 경로 JSON에 `controller_profile` 필드(§1) 추가 협의 — 미지정=precision, 값은 trajectory 산출물에 동봉해 컨트롤러 측에 전달 필요
- 07-17 00:50 — ★소비: current_state 저장 경로 규칙 반영 완료 (qc_io::resolve_rt_dir — UGRP_RT_DIR→LOCALAPPDATA\ugrp_drone, 실검증). smoother 백포팅 재검증(diagnose_smoother)은 MATLAB 큐에 추가
- 07-16 23:00 — current_state.json **v0.2 생산자 구현 완료** (C++ qc_io: jerk/traj_hash/t_on_traj/motors, 파이썬 교차 파싱 통과)
- 07-16 22:40 — docker/ 신설: Dockerfile.pathtime(경로 층) + Dockerfile.cpp(리눅스 이식성 검증) — Docker Desktop 꺼져 있어 컨테이너 검증 보류 (4979ef2)
- 07-16 22:30 — ★사용자 결정 대기: 위치 게인 A(8/3.2 유지, 추천)/B(12/4.8 절충)/C(게인 스케줄링). r8 실측: 호버 지터 범인=kp, 평탄부 없음 (d7f55c2)
- 07-16 22:00 — 위치 후보 24/10.8 관문 반려 (호버 자세 지터 0.002→0.26° 퇴행). parameters.m은 8/3.2 유지 중 — **성능 측정 세션 주의: 현행 게인 오염 없음** (c402454)
- 07-16 21:30 — C++ 몸통 완성: 제어 체인+인터페이스 4계약+모터 플랜트(진실 주입)+미션 러너+골든 도구 3종. 골든 대조 전 Gazebo 폐루프 금지 (docs/HANDOFF_CPP_GAZEBO.md)
- 07-16 20:00 — 자세 게인 채택(-85/-127.5/2500, 지터 38배), 물성 정규화(sIa/sIz/sM+관성 실측), 타당성 축A 통과. main 반영·푸시됨

## path_time 세션
- 08-01 — **scan 명령 실비행 확증** (9f4deeb): 1차 비행이 결함 3연타 적발(시작 스텝 → ±180° 요청이 -84~211° 유실 0.73 / 단일 poly7 순항 불가 2.19x / 기준 가속이 한계에 정좌 → 헌팅) → 시작 정렬 + S-사다리꼴 + 0.8x 가속 마진으로 전부 수정 → 재비행 **coverage 0.976, 추종 1.5cm, tail 0.002°**. look_at은 27.4→13.9°(반FOV 내, 기능함). ★튜닝/C++ 세션 (yaw 루프 실측): 램프 추종 0.82/1.0 rad/s 지연 + 종점 오버슈트 +51° + look_at 주시 13.9° — 기준은 클린(이음새 v=0) 확인이라 잔여는 kp_yaw 대역폭/댐핑 몫. 스캔 속도 조율(시뮬 1.0/실기 0.6, 길남 회신)도 §1 반영 완료 (3267d34)
- 08-01 — **RL seam 형식 정합: 미션 JSON 코어/옵션 분리** (사용자 지시 "충돌은 성진 쪽을 바꿔서 맞춰라") — 윤호 `interface/waypoints_config.schema.json`이 `additionalProperties:false`라 §1 확장 키(`yaw`/`waypoint_mode`/`shaper`/`controller_profile`/`waypoint_prep`/`strict`/`_comment`)를 한 개라도 섞으면 RL 측 `validate()`가 거부 → 코어 `<mission>.json`(waypoints/limits/dt만) + 사이드카 `<mission>.options.json`으로 분리. `load_mission`이 자동 병합(코어 키 사이드카 배치·양쪽 중복 = 즉사, 인라인 확장은 `_legacy_inline_options` 통지 후 하위 호환). input/ 예시 6종 분리 → **윤호 검증기 전부 VALID**(jsonschema), 테스트 169개 통과, split 미션 plan 실전 확인(lookat = fly_through+look_at 적용, accepted). 문서 3종 갱신(§1/EXTERNAL §1/INPUT_FORMAT). step_mission은 `trajectory` 입구라 RL seam 대상 외로 명시. **잔여 미정합**: 회신 경로(§7 trajectory_report) 스키마가 윤호 쪽에 없음 — RL 보상 배선의 전제, 로터 인덱스→기하·CW/CCW 매핑도 양쪽 미정
- 08-01 — **RL seam 왕복 실비행 성공 (윤호 → 성진 → MATLAB)** — 윤호 `rl/window_env.sample_scene`(seed 0) 3창 씬 → 창문 중심 웨이포인트 → `interface/schemas.WaypointsConfig.validate()` 통과 → 코어+옵션 미션(fly_through/look_at) → `plan` accepted(24.63s, hash 1470a8ec76802fce, 마진 최대 vxy 0.52) → `run_traj_baked`(T=24.6+8s). **실측: 자세 RMS 0.78°(최대 pitch 2.4°), 추종 RMS x 5mm/y 2mm/z 10mm, 종점오차 3축 0.000m, 창문 통과 0.5/0.2/0.1cm, tail 잔류 pitch·roll RMS 0.000°.** prop 각속도 평균 628/최대 820 rad/s = 정상 대역(TUNING_STATUS의 3만~9만 이슈는 구운 모델 경로엔 해당 없음 — 윤호 motor_cmd_w 스키마에 정상값이 흘러감). 모델 폴더 trajectory.mat 원복 완료, save_system 없음. 결과 `scratchpad/sim_result_baked_rlseam.mat`. **sb3/pybullet 없어 PPO 체크포인트 롤아웃은 미실행** — 웨이포인트는 정책 출력이 아니라 씬 창문 중심 대역
- 08-01 — **isaacsim_export.py 신설 + seam 실비행 2편째** — 성진 산출물을 윤호 기대 형식으로 넘기는 경계 모듈 (`§2 trajectory.json → isaacsim_trajectory.json`, `sim_result_baked.mat prop*_w → isaacsim_motor_commands.json`, 원자적 쓰기, yaw→WXYZ 쿼터니언). 실데이터로 **양쪽 윤호 스키마 VALID** (궤적 2464프레임/fps100, 모터 3741프레임/fps114.6, w ±825 rad/s 정상대역) + 윤호 dataclass 왕복·`trajectory_frame_to_T` 확인. `trajectory_hash`는 `additionalProperties:false`라 못 넣어 `*.meta.json`으로 분리(코어/옵션과 같은 패턴). 로터 인덱스→기하는 `--rotor-order`로 열어두고 실행 시 ★통지. seed1 씬(창문 2개) 실비행: 자세 RMS 0.72°/최대 pitch 2.1°/추종 RMS 5·2·10mm/종점오차 0 — seed0과 동일 정밀도 재현. **★윤호 확인 요청: 추론 어댑터 `rl/state_window_adapter.py`는 17차원(window_env) 관측인데 배포된 `ppo_window_3win.zip`은 60차원(pybullet_window_env) — 어느 쪽이 본선인지. 어느 답이든 성진 코드는 불변** (성진은 웨이포인트만 받으면 됨). pybullet은 성진 파트에 불필요해 설치 중단·정리
- 08-01 — 윤호 배포물 배치 (`window_models.tgz`, 카톡) — RL PPO 체크포인트 `reinforcement_yunho/rl/models/{ppo_window_3win.zip, pass_rate.csv}`(성공률 0.95/3창 2.85 도달), YOLO 반환물은 gpu_jobs §Job1대로 길남 몫 → `overall_gilnam/vision/models/{window_yolo11s_best.pt, results.csv}`(100 epoch). **`.pt`는 gitignore되지만 `ppo_window_3win.zip`(633KB)은 추적 대상** — 커밋 여부 사용자 판단 필요
- 07-19 밤 — **2호기 교정 v2 성공** — 공진 곡선 확정: 1.75Hz 가진 정상진폭 4.72° (1.65/1.85Hz는 1.42/1.88° — 피크 명확), 판정 기준(>0.5°) 통과 → swing_calib.json 유효 (07-18 실패의 방법론 교체가 정답이었음). **S=31.4°/(m/s²)** = 1° 상쇄에 0.032m/s² — 저크 예산 내 ~5° 상쇄 가능. 꼬리 f0 1.907Hz(대진폭)·감쇠비 0.93. 2호기 배선 재료 완비 (소비 함수 counter_params_from_calib 기구현). yaw 실비행 2편 진행 중
- 07-19 저녁 — **yunho/sim-rl-scaffold 병합** (ac29e20, 사용자 지시) — reinforcement_yunho/ 신규 49파일(+8387: Isaac Sim 씬/데이터셋/RL 환경 스캐폴드), 타 폴더 무접촉·무충돌. + yaw 검증 미션 2종 등록(정적 통과) + fly-through 촘촘 병리 조사(세그당 4s 진범 추적 — 수정 2건 커밋, 저크 폭주 미해명분은 PIPELINE_STATUS에 승계 기록). 세션 마감
- 07-19 — 사용자 부재 자율 모드 진입. **real_yaw 태핑 이미 존재 확인** (run_traj_baked.m El5, StructureWithTime) — command_fidelity 실측 경로 즉시 사용 가능, 튜닝 세션 ★(yaw 채널 요청)은 자연 해소. 교정 v2 스크립트 완성(976864a — 공진 체류 가진), EXTERNAL_INTERFACE에 command_fidelity·superseded 보상 규칙 반영
- 07-19 — **command_fidelity 구현 완료 (§7)** (ab248e5) — 시간창 waypoint 통과 오차(왕복 오인 방지 실증)/구역 이격/주시 오차(랩·동결 처리)/실측 스캔 완료율/갭 성분 dict/superseded 구분. traj_report --flight-mat 시 자동 병합. yaw 실측 채널 없으면 None (★튜닝 세션: real_yaw 태핑 요청 유지). 비상 세션 traj_pipeline.py 수정과 무충돌 (analyze/report만 작업). 테스트 128개 통과 (비상 WIP 3건 제외 — 걔들 정지거리 공식 튜닝 중, 내 변경 무관)
- 07-19 — **yaw 구현 완료 (§1 설계 그대로)** — 4모드 + scan 3정책(move/coupled/scan) + yaw 성형·게이트. 스캔 rate 불가침 원칙 코드화(성형 상한=요청 rate), 시간 왜곡 경로 snap 측정-only 강등(§7 일관). 테스트 117개 통과(비상 세션 24개 포함 회귀 0). spline_yaw 계약 불변 — 컨트롤러 수정 불필요
- 07-19 — ★튜닝/C++ 세션 (yaw 계약, 사용자 지시 "yaw 입력 없으면 default"): `spline_yaw`는 **항상 존재** (yaw 블록 미지정 시 궤적 층이 heading 자동 생성) — 컨트롤러의 "입력 없음" default는 **궤적 부재 상태(무명령 래치/부팅)에서 yaw = 현재 방위 유지** 하나만 정의하면 됨. + scan 모드 대비 yaw 스텝/램프 추종 성능(kp_yaw) 점검과 yaw 실측 로그 채널 확보(command_fidelity §7용) 요청
- 07-19 — **작업 API §8 구현 완료** (★자기소비: "다음 구현자" 항목) — 동사 6종 CLI (splice 신설: current_state 무정지 전환 + STATE_STALE 거부, check: 부작용 0), 종료 코드 0/1/2, stdout 기계용 JSON. 테스트 82개 통과. 감독자(비상 세션)가 이 CLI로 파이프라인 호출하면 됨 + command_fidelity 구현 확정사항 3건 §7 반영 (572fee4)
- 07-19 — **EXTERNAL_INTERFACE.md 신설 (팀원 공개용)** — 외부 파트가 알 것만 추림: 미션 5규칙/회신 코드표/yaw 4모드/비상 명령/조율점 4건(길남 스캔속도·윤호 토픽 매핑·태민 VIO 출처·창문 좌표 연동). 내부 기계장치는 블랙박스 처리. README 문서표 갱신
- 07-19 — **yaw 명령 인터페이스 설계 확정(§1, 구현 대기)** — 사용자 구상 "요잉하며 진행": heading/hold/look_at 3모드, 상위는 "어디 볼지"만·회전 시간표는 파이프라인. yaw 권한 최약(토크 클램프 평형 실측) 근거로 잠정 한계 rate 1.0/acc 2.0 + 게이트 확장, look_at 특이점 동결, 주시 오차는 margins 벌점. ZVD 비적용(스윙 비결합)
- 07-18 밤 — **§9 v0.2 개정: 비행 감독자 아키텍처 채택(사용자 제안)** (76f6af2) — mode 단일 소유자 flight_state.json, 철칙 3(결정 경로만/반사는 컨트롤러/하트비트 단절 시 래치 강하). ★튜닝/C++ 세션: C++ 미션 러너의 감독자 승격 협의 필요 (비상 세션 소비 07-19: 1단계는 파이썬 감독자 프로토타입으로 착수, 승격 협의는 유지)
- 07-18 밤 — **교정 비행 완주했으나 측정 실패**: 10/20cm 표준 펄스로 짐 모드가 안 가진됨 — 유발 진폭 0.07/0.21°뿐, f0=NaN(영교차 부족), 선형성 1.50(기준 1.15 초과). 현행 게인+스무딩 궤적에선 짐 모드가 사실상 억제 상태로 판단. swing_calib.json 폐기. **교정 재설계 필요** — 후보: ① 대진폭 펄스(0.5~1m) ② ZV-off 공격 왕복(지터 A 패턴) ③ 짐 직접 외란 주입(Simscape 외력 — 외란 대응이 2호기 본 목적이라 이게 정공법). 재설계는 다음 세션
- 07-18 밤 — **비상 규약 §9 확정 + 비상 전담 세션 인수인계 작성** — INTERFACE_SPEC §9 (A-1 정지/A-2 금지구역/B 회생 상태기계, 우선순위 B>A-1>A-2, 비상 레짐: ZVD 생략+마진 반납, current_state v0.3 mode 필드, 검증 의무 3편) + docs/HANDOFF_EMERGENCY.md (필독 순서·재사용 재료·물리 실측·MATLAB 함정·세션 경계·착수 순서·합격선). 비상 세션: 이 두 문서로 착수 [★소비됨: 비상 세션 착수 07-19]. 교정 비행은 병행 실행 중
- 07-18 밤 — 교정 스크립트에 **swing_calib.json 저장 추가** (f5e964f, 서브모듈) — f0/감도S/위상지연/선형성비를 파일로. 슬롯 나면 1회 실행만으로 2호기 연결 재료 완성. 매틀랩은 튜닝 세션 실행 중(2.7GB) 확인 → 교정 비행은 대기 유지
- 07-17 낮 — 세션 재개(사용자 "시작", 오늘은 라이트 모드) — 매틀랩은 튜닝 세션 선점 확인 → 2호기 교정 비행(diagnose_swing_calib.m, ~8분)은 대기/예약 등록. 참고: 미푸시 커밋 다수 — 사용자 푸시 2줄 여전히 대기
- 07-17 02:20 — **작업 API(동사 카탈로그) 설계 확정, 구현은 보류(사용자 지시)** — INTERFACE_SPEC §8: `traj_pipeline.py <verb>` 단일 진입점 6동사(plan/splice/check/feedback/estimate/status), 종료 코드 0/1/2, stdout 기계용 JSON, splice 신선도 거부(STATE_STALE). ★다음 구현자: §8 그대로 구현하면 됨 (splice CLI가 최우선 — 현재 함수만 존재)
- 07-17 02:05 — **스텝 백스톱 합격 → 매트릭스 v3 전 항목(6/6) 완결** — 추종 2.79cm / tail 0.018°, path_vis 서브미터 패치 유효 확인. 재시간화도 정상 작동(스텝 → S-커브, 팽창 x0.30, 경로 이탈 0.0cm). 세션 종료
- 07-17 02:00 — 슬롯 인수(튜닝 세션 해제 확인) → 스텝 백스톱 재비행 백그라운드 착수. 판독 기준: verification_matrix.json의 step 항목 ✅ + 추종 RMS cm급이면 매트릭스 v3 완결
- 07-17 01:55 — **세션 마감 정리**: ★소비: `controller_profile` 필드 반영 완료 — §1 스키마 + 산출물 3종(.mat/.json/meta) 동봉, 기본 precision, 테스트 74개 통과. README 모듈 지도/문서표 갱신(v0.2), PIPELINE_STATUS 매트릭스 v3 성적표 확정. 스텝 재비행은 미실행(01:40 착수분은 튜닝 세션 MATLAB 감지로 가드 중단) → 대기/예약 재등록
- 07-17 01:40 — MATLAB 슬롯 인수 시도 → 튜닝 세션 선점 감지로 가드 중단 (verify_pipeline 타 MATLAB 감지 정상 작동). 스텝 재비행은 큐 유지
- 07-17 01:05 — 스텝 실패 진범: `quadcopter_waypoints_to_path_vis.m`의 `floor(dist)*4` — **1m 미만 세그먼트 = 시각화 점 0개** → Spline 컴파일 거부. 최소 2점 가드 패치 (서브미터 경로 쓰는 모든 세션 해당). 매트릭스 v3: fly_through 다항식판 합격(추종 1.3cm/tail 0.017°), ZVD tail 8배 저감(0.12→0.015°), 질량 0.06% 재현
- 07-17 00:35 — current_state **저장 경로 규칙 확정** — 30Hz 파일은 OneDrive 밖 `env UGRP_RT_DIR → %LOCALAPPDATA%\ugrp_drone\` (repo output/은 sync 잠금으로 원자적 rename 실패 위험). INTERFACE_SPEC §5 [★소비됨: C++ 반영 완료 07-17 00:50]
- 07-17 00:30 — traj_smoother.m에 vmax 저크 스파이크 테이퍼 백포팅 (6f43567) — Python판 등가 검증 완료 [★소비됨: 튜닝 세션 MATLAB 큐에 diagnose_smoother 재검증 추가]
- 07-17 00:25 — 다항식 fly-through(통과 속도+구심 가속 BC, 중간점 정확 통과, 일직선 -30%) + 완화 계약 v0.2(클램프/재시간화, 거부 최소화) + 동적 지터 예산 + RDP 전처리. 테스트 72개
- 07-17 00:15 — current_state v0.2 스키마 확정(INTERFACE_SPEC §5) + 소비 측 스플라이스 jerk 승계 반영 (a636c2a). 매트릭스 v3(fly-through 5편+추정기) 백그라운드 비행 중
- (이하 이 세션이 직접 기록)

## 비상(emergency) 세션
- 07-19 밤 — (마감 후 추가) **SETUP.md 신설** (사용자 요청 재정정: 전체 온보딩 말고 "제어 작업물 받는 데 필요한 것"만) — 용도별 준비물 A(파이썬만)/B(MATLAB+모델 zip, 서브모듈 init 금지)/C(C++ 빌드). 루트 GETTING_STARTED.md는 요청 취지와 달라 철회(87c155d 역제거)
- 07-19 밤 — **세션 마감** (31431df) — 산출물 전부 커밋 (감독자 v0.2 / A-1 emergency 동사 / A-2 게이트·회피 / 러너 / C 감시 / verify_emergency.py, 테스트 169 통과). 잔여 = ①② 실행(MATLAB 큐: path_time 다음 순서) → ③B Simulink 반사 → ④C. 튜닝 ★(qc_zsplit_apply)은 verify_emergency가 run_traj_baked 경유라 해당 없음 확인. 다음 세션: EMERGENCY_STATUS.md "다음 일"부터
- 07-19 — ★튜닝/C++ 세션: 비상 협의 의제 3건 — ① 컨트롤러 측 하트비트 감시(철칙 3: flight_state.json written_at 나이>1.0s → 현행 궤적 완주 후 래치 호버. 기준 구현 `flight_supervisor.heartbeat_stale()`, C++ 미션 러너/qc_io에 이식 요청) ② B RECOVER 상태기계 C++ 이식(골든 확장 후 — 트리거 후보 EMERGENCY_STATUS 참조) ③ C-반사 믹서 자세 우선 배분(qc_controller.hpp 수정이라 골든 재대조 필수, 믹서 부호표 §8 기반. w_sat 임계 실측은 qc_trace Ct 0.8 열화로 비상 세션이 수행 예정)
- 07-19 — **A-2 금지 구역 + 감독자 v0.2 완료 (사용자 부재 자율 모드)** — keep_out 이격 검사(box/sphere, 전 샘플)·게이트 연동(plan/splice/emergency, KEEP_OUT_VIOLATION)·회피 재계획(재조밀화 push-out — 정관통 퇴화 2건 실측 해결)·감독자 러너(action→§8 CLI subprocess 실전 왕복)·C-모드 트리거 감시(옵트인, w_sat 실측 대기)·검증 ①② 오케스트레이터 `verify_emergency.py` 작성(미실행, MATLAB 슬롯 대기). §8 표에 emergency 동사 등재 + §9 A-2 구현 확정 추기. **테스트 169개 전체 통과** (신규 40, 회귀 0). 상세 EMERGENCY_STATUS.md
- 07-19 — **A-1 비상 정지 구현 완료** (`traj_emergency.py` + §8 `emergency` 동사 + 테스트 19개, 전체 145개 통과·회귀 0) — 실측 상태 저크 제한 최단 정지: v0=1.5에서 정지 거리 0.819m(2단 정확식 0.821 대비 -0.3%), 게이트 풀한계 통과(jPk 9.0), 비상 레짐(ZVD 생략/마진 반납/snap 측정만), STATE_STALE 거부. 잔여: MATLAB 검증 ① (대기/예약 등록)
- 07-19 — **감독자 골격 v0.1 완료** (`flight_supervisor.py` + 테스트 24개, 전체 스위트 106개 통과) — flight_state.json 단일 소유·하트비트, 미션 게이트(REJECTED_RECOVERING), 우선순위 중재(B>C>A-1>A-2, 하위는 유예), A-1/A-2 소비, B hash 무효 선언(원장), `heartbeat_stale()` 철칙 3 기준 구현. B/C 트리거 임계 후보(측정지연 0.05s 보정 포함)는 EMERGENCY_STATUS.md에 정리. 다음: A-1 emergency 동사(MATLAB 불필요분) → 검증 ①은 MATLAB 큐 대기
- 07-19 — **세션 착수** — 필독 5종(HANDOFF_EMERGENCY/§9/§8 실측/PIPELINE_STATUS/HANDOFF_CPP_GAZEBO) 정독, ★소비 2건. 임무: 감독자 + 비상 A-1/A-2/B/C + MATLAB 검증 4편. 1단계(감독자 골격, 파이썬 프로토타입) 착수 — MATLAB 불필요(튜닝 세션 점유 확인, 검증 비행은 큐 대기 예정)

## 성능 지표 세션 (2026-08-18 착수, 지니 노트북)
- 08-18 19:10 — **질량 스케줄 채택·검증 완료**(사용자 결정: 0 kg 앵커 신 게인, 0~1 kg 선형, 1 kg 불변): parameters.m/qc_mass_sched_apply/run_traj_baked/`UGRP_PKG_KG` + C++ hpp/cpp 동기(**미빌드 ★튜닝/C++ 세션**: msys64 build.ps1 + 골든 트레이스, 1 kg 무변화 기대). 배터리 15케이스(281 s) + 0 kg 미션 7편(`verify_pipeline.py --tag _0kg`) 전부 통과, 질량 스윕 0.25/0.75 보간 단조. `PERFORMANCE_SPEC.md` **v0.2** 재작성 + `PERFORMANCE_SPEC_0KG.md` v0.2, `figure/` 경우별 폴더 01~07 재정리 + README 개정, TUNING_STATUS (AA). MATLAB 해제(18:16 이후 배터리·미션 매트릭스 두 번 재점유 후 19:05 해제).
- 08-18 18:20 — 체인 완료(0 kg 튜닝 sA0.40/0.5 대조·1 kg ZVD 배터리 전부 exit 0), 결과 표 3개 PERF_STATUS에 기록, **MATLAB 해제**. 결론: 0 kg은 호버 스펙(sA 0.40)과 외란 스펙(sA 0.5도 R6 밖)을 동시에 못 만족 → 임무별 선택 또는 배터리 저장착 — 다음 세션이 사용자에게 제시
- 08-18 17:50 — **컨텍스트 리프레시 인계**: 전 상태 [PERF_STATUS.md](PERF_STATUS.md)(산출물 지도·확정 결과·다음 순서). 자율 체인 `diagnose/results/chain_0818.ps1` 실행 중(0 kg 튜닝 배터리 → sA 0.5 대조 → 1 kg ZVD → python 그림, 로그 `chain_0818.log`) — MATLAB 점유 유지. C++ FF √질량 수정은 이 머신에 컴파일러 없어 미빌드 ★튜닝/C++ 세션 빌드+골든 확인 요망. **OneDrive가 이 파일도 손상시켜 git HEAD+미커밋 diff로 복원**(`.onedrive-broken-1750` 보관) — 이 파일은 짧게 편집할 것
- 08-18 저녁 — **0 kg 재튜닝**(사용자 지시): 지터 5.4° = 5 Hz 세차 한계사이클(저속 모터 0 포화). r1 24 + r2 좌표하강 73점(21항목, 모터루프 제외) + r3/r3b 결합 36 + r4 limit_att 임계 8. 효과 축 sA↓·kd/kp↓·limit_att↓·filtM_pos·filtPz, 나머지 무효, 측정필터 단축 전부 발산. **FF 트림 오류 수정**: 56.5+44.4m 선형 → ω∝√m (`parameters.m` `bias_hover_rps` + `qc_ff_trim_apply.m` + `run_traj_baked` 훅, C++ `ffSqrt`; 1 kg 100.9 불변). 채택 후보 sA 0.40/kd:kp 0.6/limit_att 100/kp_pos 5/filtM_pos 0.005/filtPz 0.005 → 호버 0.086°(5.4→)/새그 2.5 cm(14.7→)/1 m 이동 9.8 cm(24→). 대가: 외란 0.3 N·m 이탈 18.6°(1 kg 2.3°, limit 무관). 발견: 마스크 `altitude_filtM`은 roll/pitch 측정 필터에 배선, z는 `Filter pz` 0.01 하드코딩
- 08-18 — 착수. `control_seoungjin/figure/`(그림+README) + `perf_metrics.py`/`perf_battery_plots.py` + `diagnose/perf_battery.m`(13케이스, PKG/SHAPER/FF/TUNE0KG env). 기존 로그 7편 전 스펙 합격(추종 1.5~3.4 cm). 배터리 raw 1 kg 전 항목 합격·기록 소수점 재현, 0 kg 현행 앵커 스펙 밖(대각·7 m 상승 발산). 복구: `parameters.m` OneDrive 손상 → git HEAD 복원, `diagnose/slprj` 손상 → 캐시 `%LOCALAPPDATA%/ugrp_drone/slprj_perf`. ★전 세션: OneDrive 간헐 손상 — 읽기 실패 시 `git show`로 복구

## Gazebo/C++ 검증 세션
- (미착수 — 착수 시 docs/HANDOFF_CPP_GAZEBO.md 필독, 여기 첫 줄 기록)

## 동역학 문서화 세션
- 08-18 착수·진행 중 — 기록은 **「튜닝/C++ 세션」 섹션**에 통합돼 있음 (같은 머신·같은 슬롯에서 진행). 산출물: `docs/DYNAMICS.md`(본문) / `DYNAMICS_INTRO.md`(입문) / `DYNAMICS_TALK.md`(발표) / `HANDOFF_DYNAMICS.md`(인계) + `plant_sim.py`·`compare_golden.py`(골든 트레이스 파이썬 절반)

## 외란 강건화 세션 (2026-08-22 착수) / 지연 강건화 (08-23)
- 08-23 — **★실시간 배치 정책 도출** (사용자 지적: "자세제어가 가장 우선, OS 만져서 자세 delay 최소화"). T3 실측이 갈라놓은 결론 — **자세 = 하드 실시간(16 ms, 초과 시 되돌릴 방법 없음) / 위치 = 소프트(60 ms, 스펙 감쇄로 흡수)**. 배치: ① 두 루프를 **같은 코어에 두지 말 것** — `compute_load` 가 모델링하는 부하(성형기 25.1 us/샘플, 계획기 5.3 ms/세그먼트)는 전부 위치 계층 것이라 계획 한 번에 16 ms 예산이 날아간다 ② 자세는 코어 격리(isolcpus/nohz_full)+SCHED_FIFO+mlockall (C++ 제어기는 무할당·무예외 규약이라 코드 조건은 이미 만족) ③ IMU 는 SPI+DMA (I2C 폴링 지터가 예산을 먹음) ④ 가장 확실한 건 자세 루프를 **별도 MCU** 로. 문서 `PERFORMANCE.md §8c-1`. **구현은 전무 — Gazebo/실기 이식 세션 항목**
- 08-23 — **다리·스펙 도구 신설**: `sync_delay_anchors.py`(MATLAB 결과 -> 파이썬 `_LAT_POS_ANCHORS` + C++ `SpecLatencyRule` **동시** 갱신 — 같은 표를 두 곳에 적어 두면 한쪽만 고쳐지는 사고가 반드시 난다), `export_bridge_case.py` + `diagnose/verify_bridge_sim.m`(다리 플랜트 검증, **스크립트만 준비·미실행**), `diagnose/+qctest/`(스윕 공통 헬퍼 — 투하 무력화/로깅/외란 펄스/궤적 심기, 세 스크립트 중복 제거), `scratchpad/runmat.sh`(MATLAB **창 없이** 실행 — 사용자 요청). 그림 `figure/11_delay/` 4장 + README. **테스트 253개 통과**
- 08-23 — **C++ 이식 + 언어 간 대조**: `qc::LatencyTracker` / `qc::SpecLatencyRule` / `qc::qc_spec_report` / `QcOutput.spec` / `QcInput.measAgeS`. **관측 전용**이라 제어 출력 불변(골든 트레이스 영향 없음). `tests/test_cpp_spec_parity.py` 가 120스텝 x 3조건에서 `ema_fast/ema_slow/predicted/scale/v/a/j/snap` 전부 **차 < 1e-9** 확인 + 헤더 리터럴과 파이썬 상수 대조(한쪽만 갱신 방지). ★배관 미완: `measAgeS` 를 채우는 쪽이 없고, `QcOutput.rho` 를 파이썬 조속기에 넣는 경로도 없음
- 08-23 — **재계획 인터벌 다리 `traj_bridge.py` 신설** (사용자 요구: "중간 time_path 실행하는 동안 인터벌로 이을 계획"). `replan_splice` 는 'tau 까지 옛 궤적 유효'를 전제하는데 **한계를 깎는** 경우엔 깎기로 한 순간부터 옛 궤적이 이미 위반 — 그 구멍을 메운다. 기하 불변·시계만 7차 스무드스텝으로 감속 => 금지구역 재검사 불필요, 계산 25~90 ms. **물리 한계는 어떤 감쇄폭에서도 미초과(<=0.80)**. 단 **새 한계 진입에 0.66~3.94 s** 가 걸려 재계획 예산(0.25 s)보다 항상 크다 -> ① 감쇄는 선행이어야(예측 경로 필수) ② 인계 상태가 새 한계 밖이라 새 계획은 `planner_limits` 로 만들어야 (계획기는 v(0)=v0 고정이라 v0>v_max 면 어떤 T 로도 세그먼트가 안 선다). 계획 미도착 시 경로 위 정지·래치
- 08-23 — **스펙 조속기 `spec_governor.py` 신설 = `capability.json` 의 유일한 생산자** (§5b 열린항목 1번 절반 해소). 예측(부하 모델)+실측 지연 융합 -> 배율 -> 발행, 발행 임계 0.05(경계 요동이 재계획을 유발해 부하를 또 올리는 양의 되먹임 방지). **지연도 배율 하나로** 통일(전에는 v 만 잘라 a/j/snap 이 안 따라와 '느린데 급격한' 궤적이 가능했다). 여러 배율은 **곱이 아니라 min**. ★소비자(계획기)는 여전히 안 읽음
- 08-23 — **★정정 2건**: ① **판정 게이트가 물렀다** — '외란 복귀가 존재하는가'만 보면 tau=80 ms/s=0.55 가 종단오차 4 cm 로 통과한다(복귀 **14.0 s**). 목표엔 도착하지만 진동이 안 잦아드는 상태 -> 복귀 시간 게이트(3 s, `SPEC_GATE_REC`) 신설 후 전부 재실행. ② **지연 추적기가 30표본 늦었다** — `predicted_s` 가 느린 EMA(60)만 냈다. 다리 분석상 감쇄 결정 **뒤에도** 0.7~3.9 s 가 더 걸리므로 늦은 감지는 두 번 늦는 것. `max(느린,빠른)` 으로 **24표본** 빨라졌고 비대칭 복귀는 유지. 부수 정정: 다리 스냅을 수치미분 4회로 재다 실제의 3~6배로 오측(해석적 연쇄법칙으로 교체), 비상 정지가 램프보다 먼저 켜져 급정지가 됨(그림 그려서 발견)
- 08-23 — **B상 위치(VIO) 경로 = 감쇄가 먹힌다** (`diagnose/sweep_delay_spec.m` 신설, 3 m 이동 중간에 같은 펄스). limits(tau): **0/20/30 ms -> s 1.00** (복귀 1.7~2.2 s), **40 ms -> s 0.55** (v 0.880, 종단 **0.00 cm**/복귀 **1.14 s** — 무지연 1.73 s 보다 낫다), **60 ms -> s 0.28** (v 0.448, 종단 0.16/복귀 1.98). 사용자 요구('지연이 있어도 강건성 유지')가 실측으로 성립한 지점. ★함정: **복귀가 배율에 단조롭지 않다** (60 ms 에서 6.17->12.19->11.06->7.58->1.98) — 이분 탐색 쓰면 통과 구간을 건너뛴다, **내림차순 전수 필수**. 종단오차만 보면 전부 통과(최악 1.57 cm)라 복귀 게이트 없으면 표가 무의미
- 08-23 — **A상 자세(IMU) 경로 지연 = 감쇄가 아니라 게이트** (`diagnose/sweep_delay_margin.m`). 호버 RMS 0/8/12/16/20/24 ms = 0.021/0.004/0.004/**0.211**/**2.437**/4.614도. **20 ms 는 기동을 전혀 안 한 수치** — 정지 상태의 불안정이라 궤적을 느리게 해도 안 고쳐진다. -> `LAT_ATT_CLEAN_S=12 ms`(무보정)/`LAT_ATT_MAX_S=16 ms`(초과 시 **임무 거부**, limits 0 + `mission_allowed:false`)/사이 배율 0.60. **'스펙을 낮춰 버틴다'가 안 먹히는 축이 하나 있다**는 것이 이 세션 최대 발견
- 08-22 — **C++ 동기 완료 + 미션 7편 회귀 통과**. C++ 추가분: `Pid::antiWindup`(Simulink clamping 규칙 1:1 — 포화 중 & 오차가 포화를 더 미는 방향일 때만 적분 정지, 판정은 적분 더하기 전 상태로) / `SpeedGovernor`(qc_clock_gov_apply 이식, 편차 d 를 3차=1차3단 필터, s 만 내고 tau 적분·참조조회는 호출자 몫) / `QcOutput` 진단 5종(uYaw·eYaw·rho·rhoEff·sClock = capability §5b 입력). **게이트 확인: yawAntiWindup=false 면 기존 골든과 바이트 동일**. AW 켜면 scan 재생에서 52% 행 차이(개루프 재생이라 폐루프 판정 아님) — 다만 **AW 켠 새 로그로 재검하니 모터별 스케일비 산포 2.9 → 0.3 으로 수렴**(개연성 개선). 완전 대조는 cmd 탭 추가 후
- 08-22 — **yaw 랩 + anti-windup 을 `run_traj_baked.m` 에 정식 배선** (`qc_zsplit_apply` 옆, `YAWWRAP=0`/`YAWAW=0` 로 끔). 미션 7편 회귀: **게이트 7/7 통과, 추종 RMS 전부 동일하거나 소폭 개선**(step 2.88→2.76, jitter_a 3.41→3.37, jitter_b 3.16→3.06, fly_through 2.72→2.69, look_at 2.95→2.89, scan 1.53→1.49, stop_batch 2.07 불변 / 최대 변화 −0.12 cm). 구운 .slx 무저장 규약 유지
- 08-22 — **INTERFACE_SPEC §5b 보강**: 소비 규약(낡았을 때 3단 폴백 — valid_for_s 이내 사용 / ~5s 마지막값 x0.7 / 5s 초과 정적표 0kg 열), `rho` 출처 규정(제어 주기 표본의 **구간 최대값**, 평균 쓰면 짧은 포화 놓침), 스키마 진화 규칙(추가=마이너·모르는 필드 무시 / 삭제=메이저·거부), `units` 블록, `anchor_provisional`(2kg 앵커가 1kg 복사본), `guarantees.interp_rule`(낮은 앵커 보수 채택). **열린 항목 5건 명시** — 최대는 "양쪽 끝이 안 붙었다"(컨트롤러 미기록·계획기 미독)
- 08-22 — **연산 부하 → 지연 예측 → 스펙 감쇄 신설** (`compute_load.py`): 비용 실측 `traj_smoother 25.1us/샘플(R2 0.9995)` / `plan_waypoints 5.3ms/세그` → duty → **M/D/1 체류지연**(duty→1 발산). **근거는 예상량+실측값 둘 다**(`LoadGovernor.fuse`: 모델=선행, 실측=백스톱, 차이를 `model_bias_s` 로 노출). **복귀 포함하되 비대칭**(올릴 땐 즉시 / 내릴 땐 dwell 후 지수감쇠 — 대칭이면 경계에서 요동 → 재계획 유발 → 부하 재상승 양의 되먹임). 스펙 대신 재계획 지평을 줄이는 손잡이(`horizon_for_budget`)도 제공. capability `observed.load` + reason `load_saturated` 배선
- 08-22 — **그림 `figure/10_capability/` 신설** (6장 + summary.json + README): 비용모델/부하-지연 발산/조속 시계열/능력표 감쇄 3면/쉐이퍼 정지 거동/오버슈트 막대. 기존 01~09 가 비행 실측인 것과 달리 **상위에 무엇을 노출하는가**의 그림. `make_capability_figures.py` (MATLAB 불필요). figure/README 표에 등재
- 08-22 — 테스트 **216개 전부 통과** (기존 173 + capability/latency 27 + compute_load 16)
- 08-22 — **실시간 능력 표 신설** (사용자 요구: 상위 경로 생성기가 읽을 실시간 스펙표): `capability.py` + `latency_tracker.py` + `INTERFACE_SPEC.md §5b`(schema 0.1) + 테스트 27개. 정적 능력 카드(PERFORMANCE §8b)를 기저로 **질량·외란(rho)·지연** 세 입력으로 깎아 `capability.json` 원자적 쓰기(~5Hz). 감쇄는 **시계 배율 s 하나**(v∝s, a∝s², j∝s³, snap∝s⁴)로 표현 → 상위는 limits 를 plan_waypoints 에 그대로 투입. yaw 오차를 rho 로 환산(|e|/45도)해 **회복까지 유지**가 타이머 없이 성립. 지연은 속도에만(v ≤ 0.5·track/τ). 지연 추적기는 빠른 EMA(감지)+느린 EMA(예측), 진입 연속 3표본·해제 연속 30표본 → §8c T3 '미측정' 칸을 채움. **전체 200 테스트 통과**
- 08-22 — **쉐이퍼 정지 거동 개선 (opt-in)**: 속도 상한을 `min(vmax, v_allow(남은 거리))` 로 두는 거리 연동 포락선 `smooth_stop=True`. 계단 3m 오버슈트 0.74→**0.00 cm**, 정착 12.0→**3.8 s**, 짧은 이동은 vmax 까지 안 올라감(사다리꼴→삼각형 자동 절단). 6케이스 합계 −56.8%. **다만 목표 근처에서 저크 밴드를 깨는 경우가 남아 기본값 False** (killer_step j 39 실측, 회귀 5건) — 프로덕션 전 재작업 필요. `profile='precision'(0.8·amax)|'agile'(0.9)` 추가, precision 은 기존 동작과 동일
- 08-22 — **스냅 제한은 쉐이퍼가 아니라 계획기 몫**으로 확정. 쉐이퍼에 스냅 밴드를 넣으면 한계에 붙어 달리는 입력에서 4차 지연 링잉으로 도달 오차 0.8→7~30 cm 악화(선행 감쇄·프로파일 스윕 12조합 전부 12 cm 이상). 반면 **`path_time.plan_waypoints` 는 이미 v/a/j/snap 4제약 최소시간 이진탐색이라 거리가 짧으면 한계까지 안 올라간다** (실측: 0.05 m → v 0.128/a 0.518/j 4.25, 어느 한계도 미접촉 / 1 m → a 만 바인딩 / 10 m → v 만). 역할 분담 확정: 계획기=4제약 생성, 쉐이퍼=저크 안전 포락선, `traj_gate(smax=)`=사후 검사
- 08-22 — **속도 조속기 1차 구현 + 반증**: `qc_clock_gov_apply.m`(가상 시계 = Reference/Clock1 → ∫s dt, 수술점 1곳 — 궤적 블록 5개가 모두 Min 하나에서 시간 수신) + defaults + `verify_clock_gov.m`. **기구는 설계대로**(s 1.0→0.21 매끄럽게, τ가 실시간 대비 8.5s 지연). **그러나 발산 미해결** — gov_on 도 x축 13.10 m 이탈(base 29.19 m). §5.3 "시계 멈추면 증폭할 오차 없음" 추론이 **틀림**: yaw 뒤집힘 시점의 *기존* 위치 오차를 양의 피드백이 증폭, 감속은 증가율만 줄임. **★내 판정 지표 오류**: 횡방향 y만 재서 18.2cm를 '발산 멎음'으로 오출력 → 3D로 수정. 항등 회귀도 발산 구간이라 무효(솔버 스텝 차이 1e-9 → 38.78 m 증폭) → 외란 0 조건으로 재측정 중
- 08-22 — **우선순위 정정**: 조속기는 yaw 이탈 ±90도 전제 위에서만 유효 → ① yaw 이탈 상한 확보(대역폭/limit_yaw/포화 방어)가 선행, ② 조속기, ③ 사다리 승강 순. 구현 중 교훈 2건: 3차 필터는 s가 아니라 '1로부터의 편차 d'에 걸어야 IC 0이 s(0)=1 (직접 걸면 시작부터 시계 정지), Fcn 블록 min/max 미지원 → sat01 우회
- 08-22 — **★최대 발견: 병진 루프는 yaw 잔차 ±90도가 안정성 경계** (`verify_yawdist_move.m`, 6케이스). 위치 루프가 측정 yaw로 world→body 회전을 하므로 90도 초과 시 위치 오차 피드백 부호가 뒤집혀 양의 피드백 → 발산. 실측 예외 없음: 잔차 +43.9/+41.7도 → 횡이탈 20~41cm / 잔차 -92.7/-103.7/-116.8/-182.9도 → **7~29 m 발산**. 튜닝이 아니라 구조적 조건. 부수: yaw 피크는 외란 크기로만 결정(구성 무관 동일, 포화), 감속 x0.5는 횡이탈 -49.5%(0.3N·m)/-75.0%(0.1N·m), **포화 영역 적분 배율은 유해**(40.8cm→2022cm)
- 08-22 — **검증 ③ 0.3 N·m x 3s 호버**: base/g1/g3/g5 **전부 동일**(g5 vs base 최대차 0.00138도) — yaw 출력이 ±limit_yaw=20에 25초 내내 붙는 **완전 포화**. 최대 yaw rate 171도/s, 와인드업 역방향 초과(-93.7도), 25s 내 미정착. **yaw 권한 τ_max ≈ 0.317 N·m 산출**(n0=100.9 rev/s, limit_yaw=20, plant_sim 상수) → 0.3 N·m = 권한의 94.6%. 부수: 이 계산은 C++ `mixYaw` 표가 틀렸다는 정황을 강화(표대로면 τ_max=0)
- 08-22 — **후속 설계 [docs/SPEED_GOVERNOR.md](docs/SPEED_GOVERNOR.md)** (사용자 설계): 경로 기하 불변, 참조 **가상 시계** τ̇=s(ρ)만 조절. ρ=|u_yaw|/limit_yaw(=PID 출력이 곧 외란 추정). **스냅 연속성이 s∈C³를 요구**(p⁗에 p'·s⃛ 항) → smootherstep(5차)+3차 임계감쇠. 스냅 예산 환산 **ω_s 0.39~0.57 rad/s = 정지까지 5~8초** → **조속기는 지속형 외란용, 3초 돌풍에는 무효**. 급감속(ṡ=-1/s)은 시계가 만드는 감속만 0.59 m/s² = 순항 가속의 2.5배라 자충수
- 08-22 — **MATLAB 검증 ② 합격 (scan 슬루 반증)**: 0.6 rad/s x 8s(275도) 램프에서 **슬루 구간 g3/g5 vs g1 최대차 0.00000/0.00032도** — 명령 게이트가 설계대로 닫힘. 개입은 슬루 종료 +0.43~0.58s 부터(taud+tau 지연으로 설명). 종료 후 잔차 소거는 오히려 빨라짐(t=20s 12.1→4.8도). ※ 초판 판정 로직이 비교 창을 전 구간으로 잡아 정상 동작을 '불합격'으로 오출력 → 슬루 구간 한정으로 수정함, 원 로그 볼 때 주의. **부수 발견**: yaw 슬루 추종 지연 32.8도(0.95s분)·오버슈트 27~29도 — 약권한 채널 증거, 적분으로는 못 고침. scan 미션 설계가 이 지연을 전제하는지 확인 필요
- 08-22 — **MATLAB 검증 ① 합격**: 지속외란 0.01 N·m 25s 호버에서 정상편차 base 7.676도 → g3 0.719도(-90.6%) / g5 0.293도(-96.2%), **평시 호버 지터 0.0108도 세 구성 완전 동일**(상수 Ki 인상의 맞교환 없음). 항등 회귀 g1 vs base **최대 yaw 차 0.00000도** → 외부 P/I/D/N 포트 순서(e,P,I,D,N)도 이로써 실증. 최대이탈은 39.4→33.1도로 개선 작음(과도는 P/D 몫, 적분은 정상편차 항). `diagnose/verify_yawdist.m`, 결과 `diagnose/results/verify_yawdist.mat`. **.slx 무저장 확인**(mtime 08-02 불변)
- 08-22 — 정찰 `diagnose/probe_yaw_pid.m`: `Control Yaw`=PID 1dof 마스크, **Parallel/Forward Euler/AntiWindup none** → C++ `Pid` 와 구조 동일. `ControllerParametersSource='external'` 지원(Inport 1→5). 경로 A(I 포트 외부화) 확정 → `Scripts_Data/qc_yawdist_apply.m` + `qc_yawdist_defaults.m` 작성. C++ relax 도 Simulink Rate Limiter 와 같은 선형 기울기 제한으로 정렬(항등 유지)
- 08-22 — **yaw 외란 적응 적분 C++ 구현 + 항등 확인** (사용자 설계: 외란 감지 시 I 한시 조정). `Pid::kiScale`(ki가 아니라 적분 *누적률*만 배율 → 출력 점프 없음) + `YawDistI`(지속성 LPF ē + `|psi_ref_dot|` 슬루 게이트 이중 판정). 기본 `yawDistGmax=1` → scan_0kg 골든 2376스텝 신/구 exe 출력 **바이트 동일**. 설계·검증 계획 [docs/YAW_DISTURBANCE_I.md](docs/YAW_DISTURBANCE_I.md). 잔여: Simulink `qc_yawdist_apply.m`(MATLAB 슬롯) + 지속외란/scan 반증 스윕
- 08-22 — ★전 세션(성능 지표/지니 노트북): **08-18 19:10 parameters.m 변경분이 이 머신에 없음** — `filtPz_mass`/`nl_gmax`/`nl_e0`/`nl_e1`/`bias_hover_rps`/`UGRP_PKG_KG` 전부 미정의인데 `qc_mass_sched_apply`/`qc_nl_att_apply`/`qc_ff_trim_apply` 는 이 값들을 참조 → **현 상태로 실행하면 즉사**. run_traj_baked 훅도 `qc_zsplit_apply` 하나뿐. 지니 노트북에서 parameters.m + run_traj_baked.m 가져올 것 (OneDrive 부분 동기 의심)
- 08-22 — 관측: `qc_controller.hpp` `mixYaw={-1,+1,-1,+1}` × `mixDir={+1,-1,-1,+1}` 조합은 반토크 1차항이 상쇄됨(Σ mixYaw=0). 08-18 골든 확정은 x/y런 roll/pitch 상관이라 yaw 열은 미검정 → 대기/예약의 yaw 실비행 1편에서 같이 판정

## 대기/예약 (세션 무관)
- [MATLAB] 지연 세션: **0 kg 위치 지연표** (`SPEC_PKG=0 sweep_delay_spec`) — 1 kg 만 쟀다. 0 kg 은 과민 구간이라 외삽 금지. 담을 자리(질량별 지연표)도 아직 없음
- [MATLAB] 지연 세션: **다리 플랜트 검증** (`export_bridge_case.py` -> `verify_bridge_sim.m`) — 다리는 아직 파이썬 안에서만 확인됨. A 전속유지 vs B 다리 비교
- [MATLAB] 튜닝 세션: **전체 파이프라인 스텝응답** (`diagnose/verify_step_pipeline.m`, 성형기 포함 rise/정착/오버슈트/SSE, 4심 ~8분, 사용자 요청) — path_time 큐 종료 대기 자동 실행 걸어둠 (감시 스크립트). agile 전용 1차식·관문은 07-19 완결
- [MATLAB] path_time 세션: 2호기 교정 v2 **공진 체류 가진** (`diagnose/diagnose_swing_calib2.m`, 976864a — 스크립트 완성, f0 스윕 3점 ~8분). 방법론 변경: 외력 주입이 아니라 궤적 가진 (2호기 액추에이터 = 드론 가속이라 교정도 같은 경로). 슬롯 나면 실행 → swing_calib.json
- [MATLAB] path_time 세션: yaw 실비행 1편 (scan coupled 미션 — 컨트롤러 yaw 추종 + real_yaw 실측 + command_fidelity 왕복 검증, ~4분). 교정 v2 다음 순위
- [MATLAB] 비상 세션: 검증 ①·② (`python verify_emergency.py` — A-1 정지 오버슈트/드리프트 + A-2 회피 실측 이격, MATLAB 3회 ~20분, 타 MATLAB 가드 내장) — 슬롯 해제 시 실행. 첫 실행이라 로그 전체 확인 필요
- [MATLAB] 외란 강건화 세션: yaw 외란 적응 적분 검증 (`docs/YAW_DISTURBANCE_I.md` §6 — 회귀 7편 + 지속외란 매트릭스 + **scan 반증 시험**(게이트가 슬루를 실제로 배제하는지) + gmax 스윕). Simulink `qc_yawdist_apply.m` 작성이 선행
- [사용자] 지니 노트북에서 `Scripts_Data/quadcopter_package_parameters.m` + `run_traj_baked.m` (08-18 19:10판) 회수 — 없으면 08-18 0kg 재튜닝 산출물 전체가 재현 불가
- [사용자] 푸시 2줄 (서브모듈 → 부모 순서)
- [Docker] qc-cpp 컨테이너 빌드 (Desktop 기동 시)
