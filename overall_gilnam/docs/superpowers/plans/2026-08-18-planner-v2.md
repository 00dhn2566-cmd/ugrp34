# 웨이포인트 계획기 v2 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 윤호 프로토타입 래퍼(`prototype_demo/planner.py`)가 구현한 실전 요구사항 6종(법선 수평·z 클램프·후진 완화·정렬점·정지점·재계획)과 구조화 경고, `assemble_window_map` 승격을 우리 계획기 본체에 흡수 — 기존 `plan_waypoints` 계약은 불변.

**Architecture:** `window_waypoint_planner.py`에 `Plan` 데이터클래스 + `plan_waypoints_v2` 추가, `plan_waypoints`는 v2를 감싸 `WaypointsConfig` 반환. `crossing_warnings`는 dict 반환 + `format_warning`. `assemble_window_map`은 planning으로 이동, integration은 re-export. v2 동작은 cfg 키 존재 시에만 활성(키 없으면 v1 동일 — 회귀 테스트).

**Tech Stack:** Python, numpy, pyyaml, pytest. 새 의존성 금지.

**Spec:** `overall_gilnam/docs/superpowers/specs/2026-08-18-planner-v2-design.md`

## Global Constraints

- Python: `C:\Users\user\anaconda3\python.exe` (PATH `python`은 스토어 스텁 — 금지).
- 테스트: `overall_gilnam/planning`에서 `-m pytest tests/ -q` (기존 15), `overall_gilnam/integration`에서 (기존 3). vision의 `test_toy_and_eval` 실패는 기존 환경 문제 — 무관.
- 다른 팀원 코드 무수정. 윤호 브랜치는 **읽기 참조만** (`git show origin/feat/pybullet-prototype-pipeline:prototype_demo/planner.py`).
- 상수 기본값 (윤호 래퍼 그대로): `stop_ahead 0.6`, `align_back 0.45`, `max_passes 4`, `shrink [1.0, 0.75, 0.55, 0.4]`, `gate_z [0.5, 1.9]`, `force_horizontal_normal true`.
- 후진 정의: fwd = −n̂ₖ₊₁, back = (이탈ₖ − 접근ₖ₊₁)·fwd, back > 0이면 후진량. 재계획 최선 = (경고 수, 후진량) 사전식 최소. `ok` = 경고 0 ∧ 후진 ≤ 1e-6.
- 정렬점 조건: prev_exit→approach 구간이 창문 평면을 가로지르고(da·db<0) 교차점이 개구부(여유 적용) 밖이면 `approach + align_back·n̂` 삽입 (approach 앞에).
- 주석·docstring 한국어, 커밋 메시지 태스크 문구 그대로 + 트레일러:

```
Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```

---

### Task 1: 구조화 경고 + assemble 승격 (계약 정비, 동작 불변)

**Files:**
- Modify: `overall_gilnam/planning/window_waypoint_planner.py`
- Modify: `overall_gilnam/integration/e2e_rehearsal.py` (assemble 본문 제거 → import re-export)
- Test: `overall_gilnam/planning/tests/test_planner.py` (추가), `overall_gilnam/integration/tests/test_e2e.py` (기존 그대로 — import 경로 유지 확인)

**Interfaces:**
- Produces: `crossing_warnings(waypoints, windows, clearance_margin) -> list[dict]` (키: `order_index, color, seg_index, a, b, u, v, half_w, half_h`), `format_warning(w: dict) -> str` (기존 문자열과 동일 형식), `assemble_window_map(recon) -> (window_map, failed)` (planning에 정의, integration은 `from window_waypoint_planner import assemble_window_map`로 재노출).
- Consumes: 기존 `normal_from_corners`, `UP`.

- [ ] **Step 1: 실패하는 테스트 추가** — `tests/test_planner.py`에 이어서

```python
from window_waypoint_planner import assemble_window_map, format_warning


def test_crossing_warnings_structured():
    win = {"order_index": 0, "color": "red", "center": [5.0, 0.0, 1.5],
           "normal": [-1.0, 0.0, 0.0], "size_wh": [1.0, 1.0]}
    seg = [[0.0, 2.0, 1.5], [10.0, 2.0, 1.5]]
    warns = crossing_warnings(seg, [win], 0.35)
    assert len(warns) == 1
    w = warns[0]
    assert w["order_index"] == 0 and w["seg_index"] == 0
    assert w["u"] == pytest.approx(2.0, abs=1e-6) and abs(w["v"]) < 1e-9
    assert w["half_w"] == pytest.approx(0.15) and w["half_h"] == pytest.approx(0.15)
    s = format_warning(w)
    assert "order_index=0" in s and "개구부 밖" in s


def test_assemble_window_map_moved_to_planning():
    recon = {
        1: {"color": "green", "n_pairs": 5, "corners_3d_est": np.array(
            [[4.0, 0.6, 2.0], [4.0, -0.6, 2.0], [4.0, -0.6, 1.0], [4.0, 0.6, 1.0]])},
        0: {"color": "red", "corners_3d_est": None, "n_pairs": 0},
    }
    wmap, failed = assemble_window_map(recon)
    assert failed == [0] and [w["order_index"] for w in wmap["windows"]] == [1]
    assert np.dot(wmap["windows"][0]["normal"], [-1.0, 0.0, 0.0]) > 0.999  # 부호 확정 공식
```

- [ ] **Step 2: 실패 확인**

Run (`overall_gilnam/planning/`): `C:\Users\user\anaconda3\python.exe -m pytest tests/ -v`
Expected: 신규 2개 FAIL — `ImportError: cannot import name 'assemble_window_map'`

- [ ] **Step 3: 구현**

`crossing_warnings`의 `warns.append(f"경고: ...")`를 dict append로 교체하고 `format_warning` 추가:

```python
            if abs(u) > half_w or abs(v) > half_h:
                warns.append({
                    "order_index": w["order_index"], "color": w.get("color", "?"),
                    "seg_index": seg_i, "a": a.tolist(), "b": b.tolist(),
                    "u": float(u), "v": float(v), "half_w": float(half_w), "half_h": float(half_h),
                })
    return warns


def format_warning(w):
    """구조화 경고 → 사람용 문자열 (v1 문자열 형식 유지)."""
    return (f"경고: 구간 {np.round(w['a'], 2).tolist()}→{np.round(w['b'], 2).tolist()}가 "
            f"창문 order_index={w['order_index']}({w['color']}) 평면을 "
            f"개구부 밖(u={w['u']:.2f}, v={w['v']:.2f})에서 교차")
```

(루프를 `for seg_i, (a, b) in enumerate(zip(waypoints, waypoints[1:])):`로.)

`plan_waypoints` 안의 경고 호출은 `for msg in crossing_warnings(...): warn(format_warning(msg))`로. CLI `main()`도 동일.

`assemble_window_map`을 `e2e_rehearsal.py`에서 잘라 `window_waypoint_planner.py`(ordered_open_windows 아래)에 붙이고 — docstring에 "reconstruct_windows 결과 → §6.2 창문 맵. 실전 소비처(윤호 pipeline_demo) 생겨 planning으로 승격 (2026-08-18)" 추가. `e2e_rehearsal.py`의 planner import 목록에 `assemble_window_map` 추가 (re-export — 기존 `from e2e_rehearsal import assemble_window_map` 테스트가 그대로 통과해야 함). e2e_rehearsal의 `crossing_warnings` 소비(`warnings += crossing_warnings(...)`)는 dict 리스트가 되므로 `run_scale`의 `warnings` 리스트는 `[format_warning(x) for x in ...]`로 문자열 유지 (반환 키 `warnings`는 문자열 리스트 계약).

- [ ] **Step 4: 통과 확인** — planning 17 passed, integration 3 passed (E2E 게이트 불변).

- [ ] **Step 5: 커밋**

```bash
git add overall_gilnam/planning/window_waypoint_planner.py overall_gilnam/planning/tests/test_planner.py overall_gilnam/integration/e2e_rehearsal.py
git commit -m "planning: 구조화 경고(format_warning 분리) + assemble_window_map planning 승격 — 동작 불변"
```

---

### Task 2: 게이트 안전 (법선 수평·z 클램프) — cfg 활성

**Files:**
- Modify: `overall_gilnam/planning/window_waypoint_planner.py`, `overall_gilnam/planning/planner_limits.yaml`
- Test: `tests/test_planner.py` (추가)

**Interfaces:**
- Produces: `resolve_normal(window, force_horizontal=False) -> np.ndarray` (기존 3분기 + 수평화), `gate_points(window, d_app, d_exit, clearance_margin, force_horizontal=False, gate_z=None)`.
- yaml 신규 키: `force_horizontal_normal: true`, `gate_z: [0.5, 1.9]` (주석: 윤호 실측 — 기울어진 복원 법선으로 접근점 지면 아래 → 추락).

- [ ] **Step 1: 실패하는 테스트 추가**

```python
from window_waypoint_planner import resolve_normal


def test_resolve_normal_forces_horizontal():
    tilted = {"order_index": 0, "color": "red", "center": [5, 0, 1.5], "size_wh": [1, 1],
              "normal": [-0.8, 0.0, -0.6]}                     # 35°쯤 기울어진 복원 법선
    n = resolve_normal(tilted, force_horizontal=True)
    np.testing.assert_allclose(n, [-1.0, 0.0, 0.0], atol=1e-9)
    n0 = resolve_normal(tilted, force_horizontal=False)
    assert abs(n0[2]) > 0.5                                    # 끄면 원본 유지


def test_resolve_normal_keeps_vertical_when_horizontal_part_vanishes():
    vertical = {"order_index": 0, "color": "red", "center": [5, 0, 1.5], "size_wh": [1, 1],
                "normal": [0.0, 0.0, -1.0]}
    n = resolve_normal(vertical, force_horizontal=True)
    np.testing.assert_allclose(n, [0.0, 0.0, -1.0])           # 판단 불가 → 원본


def test_gate_points_z_clamp():
    low = {"order_index": 0, "color": "red", "center": [5.0, 0.0, 0.3], "size_wh": [1, 1],
           "normal": [-1.0, 0.0, 0.0]}
    a, e = gate_points(low, 1.5, 1.0, 0.35, gate_z=(0.5, 1.9))
    assert a[2] == pytest.approx(0.5) and e[2] == pytest.approx(0.5)
    a2, _ = gate_points(low, 1.5, 1.0, 0.35)                   # 미지정이면 클램프 없음
    assert a2[2] == pytest.approx(0.3)
```

- [ ] **Step 2: 실패 확인** — `ImportError: resolve_normal`

- [ ] **Step 3: 구현**

`gate_points`의 법선 3분기를 `resolve_normal`로 추출:

```python
def resolve_normal(window, force_horizontal=False):
    """창문 법선 결정: 명시 normal → corners_3d 유도 → 에러. force_horizontal이면 수평 성분만 남겨
    정규화(창문 수직 씬 가정 — 복원 법선 기울어짐으로 게이트가 지면 아래로 가던 실측 사고 방지,
    윤호 프로토타입 2026-08-18). 수평 성분이 거의 0이면 판단 불가 → 원본 유지."""
    ident = f"order_index={window.get('order_index')}({window.get('color', '?')})"
    if "normal" in window:
        n = np.asarray(window["normal"], dtype=float)
        n_len = float(np.linalg.norm(n))
        if n_len < 1e-9:
            raise ValueError(f"창문 {ident}: normal이 영벡터 — 접근측 판정 불가")
        n = n / n_len
    elif "corners_3d" in window:
        n = normal_from_corners(window["corners_3d"])
    else:
        raise ValueError(f"창문 {ident}: normal·corners_3d 모두 부재 — 접근측 판정 불가")
    if force_horizontal:
        h = np.array([n[0], n[1], 0.0])
        if np.linalg.norm(h) >= 1e-9:
            n = h / np.linalg.norm(h)
    return n
```

`gate_points(window, d_app, d_exit, clearance_margin, force_horizontal=False, gate_z=None)`: 여유 검사 후 `n = resolve_normal(window, force_horizontal)`, 게이트 계산 후 `if gate_z is not None: 각 게이트 z를 np.clip`. `crossing_warnings`의 법선 결정도 `resolve_normal(w)`(try/except ValueError → continue)로 통일.

yaml 추가:
```yaml
# --- v2 (2026-08-18, 윤호 프로토타입 실측 요구사항 흡수 — 키를 지우면 v1 동작) ---
force_horizontal_normal: true   # 복원 법선 기울어짐 → 접근점 지면 아래 추락 실측. 창문 수직 씬 가정
gate_z: [0.5, 1.9]              # 게이트 z 안전대역 [m]
```

- [ ] **Step 4: 통과 확인** — planning 20 passed.

- [ ] **Step 5: 커밋**

```bash
git add overall_gilnam/planning/window_waypoint_planner.py overall_gilnam/planning/planner_limits.yaml overall_gilnam/planning/tests/test_planner.py
git commit -m "planning v2: resolve_normal(수평 강제)·게이트 z 클램프 — cfg 활성, 기본 꺼짐 호환"
```

---

### Task 3: `plan_waypoints_v2` — 후진·정렬점·정지점·재계획 + `plan_waypoints` 위임

**Files:**
- Modify: `overall_gilnam/planning/window_waypoint_planner.py`, `overall_gilnam/planning/planner_limits.yaml`
- Test: `tests/test_planner.py` (추가)

**Interfaces:**
- Produces: `@dataclass Plan(waypoints: list[list[float]], labels: list[str], warnings: list[dict], passes: int, shrink: float, backtrack_m: float)` + `ok` 프로퍼티; `plan_waypoints_v2(drone_state, window_map, cfg) -> Plan`; `plan_waypoints`는 내부에서 v2 호출 후 `WaypointsConfig(waypoints=plan.waypoints, ...)`, 경고는 `warn(format_warning(w))`.
- yaml 키: `stop_ahead: 0.6`, `align_back: 0.45`, `max_passes: 4`, `shrink: [1.0, 0.75, 0.55, 0.4]`.
- **호환 규칙**: cfg에 `stop_ahead`가 없으면 v1 경로(정지점·정렬점·재계획 없음, 웨이포인트 = start + 2N) — 기존 테스트 `len == 7`이 그대로 통과해야 함.

- [ ] **Step 1: 실패하는 테스트 추가**

```python
from window_waypoint_planner import Plan, plan_waypoints_v2

V2 = {"d_app": 1.5, "d_exit": 1.0, "clearance_margin": 0.35,
      "limits": {"v_max": 1.6, "a_max": 1.6, "j_max": 8.0, "snap_max": 40.0}, "dt": 0.01,
      "force_horizontal_normal": True, "gate_z": [0.5, 1.9],
      "stop_ahead": 0.6, "align_back": 0.45, "max_passes": 4, "shrink": [1.0, 0.75, 0.55, 0.4]}


def _win(i, x, y=0.0, n=(-1.0, 0.0, 0.0), wh=(1.0, 1.0)):
    return {"order_index": i, "color": ["red", "green", "blue"][i], "center": [x, y, 1.5],
            "normal": list(n), "size_wh": list(wh)}


def test_v2_labels_stop_point_and_ok():
    wmap = {"windows": [_win(0, 4.0), _win(1, 9.0), _win(2, 14.0)]}   # 간격 5m — 후진 없음
    p = plan_waypoints_v2(STATE, wmap, V2)
    assert isinstance(p, Plan) and p.ok and p.passes == 1 and p.shrink == 1.0
    assert p.labels == ["start", "approach0", "exit0", "approach1", "exit1", "approach2", "exit2", "stop"]
    np.testing.assert_allclose(p.waypoints[-1], [14.0 + 1.0 + 0.6, 0.0, 1.5])  # exit2 − stop_ahead·n


def test_v2_backtrack_detected_and_relaxed():
    wmap = {"windows": [_win(0, 4.0), _win(1, 5.5)]}    # 간격 1.5m < d_app+d_exit=2.5 → 후진
    p = plan_waypoints_v2(STATE, wmap, V2)
    assert p.passes > 1                                  # 재계획 발생
    assert p.shrink < 1.0
    # 완화 후 후진 없거나, 못 없앴으면 정직 보고
    assert p.ok or p.backtrack_m > 0


def test_v2_align_point_inserted():
    # 창문1이 옆으로 크게 비켜 있어 exit0→approach1 직선이 창문1 평면을 개구부 밖에서 뚫음
    wmap = {"windows": [_win(0, 4.0, 0.0), _win(1, 8.0, 3.0)]}
    p = plan_waypoints_v2(STATE, wmap, V2)
    assert "align1" in p.labels
    i = p.labels.index("align1")
    assert p.labels[i + 1] == "approach1"
    # 정렬점 = approach1 + align_back·n̂
    ap = np.asarray(p.waypoints[i + 1]); al = np.asarray(p.waypoints[i])
    np.testing.assert_allclose(al - ap, np.array([-1.0, 0.0, 0.0]) * 0.45, atol=1e-9)


def test_plan_waypoints_v1_path_unchanged_without_v2_keys():
    cfg_v1 = {k: V2[k] for k in ("d_app", "d_exit", "clearance_margin", "limits", "dt")}
    wc = plan_waypoints(STATE, WMAP3, cfg_v1)
    assert len(wc.waypoints) == 7                          # v1과 동일: start + 2N, 정지점 없음


def test_plan_waypoints_wraps_v2_when_keys_present():
    wc = plan_waypoints(STATE, {"windows": [_win(0, 4.0), _win(1, 9.0), _win(2, 14.0)]}, V2)
    wc.validate()
    assert len(wc.waypoints) == 8                          # + stop
```

- [ ] **Step 2: 실패 확인** — `ImportError: Plan`

- [ ] **Step 3: 구현** (윤호 `_build`/`plan` 로직을 우리 코드 스타일로 재구현 — 상수는 cfg에서)

```python
from dataclasses import dataclass, field


@dataclass
class Plan:
    """v2 계획 결과. ok = 경고 없음 ∧ 후진 없음."""
    waypoints: list = field(default_factory=list)
    labels: list = field(default_factory=list)
    warnings: list = field(default_factory=list)   # crossing_warnings dict
    passes: int = 0
    shrink: float = 1.0
    backtrack_m: float = 0.0

    @property
    def ok(self):
        return not self.warnings and self.backtrack_m <= 1e-6


def _inside_opening(pt, window, n, margin):
    center = np.asarray(window["center"], dtype=float)
    wa = np.cross(UP, n)
    if np.linalg.norm(wa) < 1e-9:
        return True
    wa = wa / np.linalg.norm(wa)
    d = pt - center
    return (abs(d @ wa) <= window["size_wh"][0] / 2.0 - margin
            and abs(d @ UP) <= window["size_wh"][1] / 2.0 - margin)


def _build_v2(windows, start, d_app, d_exit, margin, force_h, gate_z, align_back, stop_ahead):
    pts, labels, backtrack, prev_exit = [np.asarray(start, dtype=float)], ["start"], 0.0, None
    for k, w in enumerate(windows):
        ap, ex = gate_points(w, d_app, d_exit, margin, force_horizontal=force_h, gate_z=gate_z)
        n = resolve_normal(w, force_h)
        if prev_exit is not None:
            center = np.asarray(w["center"], dtype=float)
            da, db = float((prev_exit - center) @ n), float((ap - center) @ n)
            if da * db < 0:                                     # 이미 평면을 가로지름
                hit = prev_exit + (ap - prev_exit) * (da / (da - db))
                if not _inside_opening(hit, w, n, margin):
                    pts.append(ap + align_back * n); labels.append(f"align{k}")
            back = float((prev_exit - ap) @ (-n))               # 진행방향 = −n
            if back > 0:
                backtrack = max(backtrack, back)
        pts.append(ap); labels.append(f"approach{k}")
        pts.append(ex); labels.append(f"exit{k}")
        prev_exit = ex
    n_last = resolve_normal(windows[-1], force_h)
    pts.append(prev_exit - stop_ahead * n_last); labels.append("stop")
    return pts, labels, backtrack


def plan_waypoints_v2(drone_state, window_map, cfg):
    """v2: 후진 완화·정렬점·정지점·재계획 (윤호 프로토타입 래퍼 요구사항 흡수, 2026-08-18)."""
    windows = ordered_open_windows(window_map)
    if not windows:
        raise ValueError("열린 창문이 없음 — 계획할 대상 없음")
    force_h = bool(cfg.get("force_horizontal_normal", False))
    gate_z = tuple(cfg["gate_z"]) if cfg.get("gate_z") else None
    shrinks = list(cfg.get("shrink", [1.0]))
    best = None
    for i in range(int(cfg.get("max_passes", 1))):
        s = shrinks[min(i, len(shrinks) - 1)]
        pts, labels, back = _build_v2(windows, drone_state["position"], cfg["d_app"] * s, cfg["d_exit"] * s,
                                      cfg["clearance_margin"], force_h, gate_z,
                                      cfg["align_back"], cfg["stop_ahead"])
        warns = crossing_warnings([p.tolist() for p in pts], windows, cfg["clearance_margin"])
        plan = Plan([[float(v) for v in p] for p in pts], labels, warns, i + 1, s, back)
        if best is None or (len(plan.warnings), plan.backtrack_m) < (len(best.warnings), best.backtrack_m):
            best = plan
        if plan.ok:
            break
    return best
```

`plan_waypoints`: `if "stop_ahead" in cfg:` → v2 경로(`plan = plan_waypoints_v2(...)`, `points = plan.waypoints`, 경고는 `plan.warnings`를 `format_warning`으로 warn) / else 기존 v1 경로 그대로. 이후 `WaypointsConfig(...)`·`validate()` 동일.

yaml 추가:
```yaml
stop_ahead: 0.6                 # 마지막 이탈점 후 정지점까지 [m]
align_back: 0.45                # 통과축 정렬점을 접근점 뒤로 [m]
max_passes: 4                   # 후진/경고 시 재계획 최대 횟수
shrink: [1.0, 0.75, 0.55, 0.4]  # 패스별 standoff 축소 계수
```

- [ ] **Step 4: 통과 확인** — planning 25 passed, integration 3 passed (E2E는 planner_limits.yaml을 읽으므로 이제 v2 경로 — scale 0 게이트는 게이트점 오차 비교라 정지점 추가와 무관하지만 **인덱싱 `1+2i`가 정렬점 삽입 시 깨질 수 있음** → integration test 실패 시 `run_scale`의 게이트점 추출을 labels 기반(`approach{i}`/`exit{i}`)으로 바꾸는 최소 수정 허용. 그 경우 커밋에 e2e_rehearsal.py 포함).

- [ ] **Step 5: 커밋**

```bash
git add overall_gilnam/planning/ overall_gilnam/integration/e2e_rehearsal.py
git commit -m "planning v2: plan_waypoints_v2 — 후진 완화·정렬점·정지점·재계획 루프, plan_waypoints 위임(v1 호환)"
```

---

### Task 4: 윤호 래퍼 동치 회귀 테스트 + 문서

**Files:**
- Test: `tests/test_planner.py` (추가)
- Modify: `overall_gilnam/planning/window_waypoint_planner.py` (docstring), `overall_gilnam/docs/superpowers/plans/2026-08-08-window-waypoint-planner.md`(무변경), `overall_gilnam/vision/README.md`(무변경) — 계획기 README가 없으므로 모듈 docstring에 v2 요약 추가
- 커밋 포함: spec·plan (`overall_gilnam/docs/superpowers/{specs,plans}/2026-08-18-planner-v2*.md`)

**Interfaces:**
- Consumes: Task 3의 `plan_waypoints_v2`. 참조: 윤호 `prototype_demo/planner.py._build` (읽기 전용 — 동일 입력에서 그 로직으로 손계산한 기대값을 테스트에 고정).

- [ ] **Step 1: 동치 회귀 테스트** — 윤호 `_build`를 스크래치에서 실행(`git show origin/feat/pybullet-prototype-pipeline:prototype_demo/planner.py > <스크래치>/yunho_planner.py`, 그 파일은 `window_waypoint_planner`의 `normal_from_corners`·`crossing_warnings`를 import하므로 planning 폴더를 sys.path에 넣어 실행 — 단 그의 `crossing_warnings` 호출은 문자열 리스트를 기대하나 우리 것은 dict — `len()`만 쓰므로 호환)해 아래 두 씬의 웨이포인트를 뽑아 **고정값**으로 테스트에 박는다:

```python
def test_v2_matches_yunho_wrapper_reference():
    # 윤호 prototype_demo/planner.py._build를 동일 입력·동일 상수로 실행한 결과를 고정 (2026-08-18)
    wmap = {"windows": [_win(0, 4.0, 0.0), _win(1, 8.0, 3.0), _win(2, 13.0, 3.0)]}
    p = plan_waypoints_v2({"position": [0.0, 0.0, 1.0]}, wmap, V2)
    assert p.labels == [<실행해서 얻은 라벨 리스트>]
    np.testing.assert_allclose(p.waypoints, [<실행해서 얻은 좌표>], atol=1e-6)
```

(자리표시자는 실행값으로 채운다 — 실행 결과와 우리 v2가 다르면 **우리 구현을 고쳐 일치시킨다**, 기대값을 조정하지 않는다. 단 그의 `plan()`은 windows 정렬에 `w.get("order_index", 0)`, 시작점 z=1.0 등 세부가 있으니 동일 입력으로 맞출 것.)

- [ ] **Step 2: 실행·확인** — planning 26 passed.

- [ ] **Step 3: docstring** — 모듈 docstring 끝에 v2 요약 5줄(흡수 항목 A~H, cfg 키로 활성, 윤호 `prototype_demo/planner.py`는 `plan_waypoints_v2`로 대체 가능).

- [ ] **Step 4: 커밋**

```bash
git add overall_gilnam/planning/ overall_gilnam/docs/superpowers/specs/2026-08-18-planner-v2-design.md overall_gilnam/docs/superpowers/plans/2026-08-18-planner-v2.md
git commit -m "planning v2: 윤호 래퍼 동치 회귀 테스트 + docstring, spec/plan"
```

---

## Self-Review 기록

- 스펙 커버리지: G·H(T1), A·B(T2), C·D·E·F(T3), 게이트 2 동치(T4), 게이트 1 회귀(T3 v1 경로 테스트 + 기존 스위트), 게이트 3 단위(T2·T3) — 전 항목 대응.
- 자리표시자: T4 고정값은 실행값 기입 지시 (실행 전 부재) — 기대값 조정 금지 규칙 명시.
- 타입 일관성: `resolve_normal`(T2 정의 = T3 사용), `crossing_warnings` dict(T1 정의 = T3 `Plan.warnings`), `Plan`(T3 정의 = T4 사용), `gate_points` 확장 시그니처(T2 = T3 `_build_v2`) 대조 완료. E2E 인덱싱 위험은 T3 Step 4에 명시.
