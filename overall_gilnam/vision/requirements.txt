# vision 파트 의존성 (Python 3.13 기준, 2026-07-03)
# 설치: python -m pip install -r requirements.txt
numpy==2.5.0
opencv-python==5.0.0.93
PyYAML==6.0.3
pytest==9.1.1

# 학습 단계에서 추가 예정 (model_decisions #1: 버전 핀 고정 필수)
# ultralytics==<학습 시작 시 최신 안정 버전으로 확정>
