# vision 파트 의존성 (Python 3.13 기준, 2026-07-03)
# 설치: python -m pip install -r requirements.txt
numpy==2.5.0
opencv-python==5.0.0.93
PyYAML==6.0.3
pytest==9.1.1

# 학습 의존성 (model_decisions #1: 버전 핀 고정 — 2026-07-04 리허설에서 확정)
# torch는 ultralytics의 전이 의존성이라 별도 핀 불필요 (리허설 당시 2.12.1+cpu)
ultralytics==8.4.87
