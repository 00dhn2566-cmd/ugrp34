# 창문 탐지 데이터셋 · 인터페이스 규격 (v0.2 — 확정본)

> 목적: 이미지 처리기(창문 탐지)의 **데이터셋 생성 규격**과 **VIO 전달 규격**을 하나의 문서로 통일한다.
> 이 문서 한 벌이 (1) 조윤호의 Isaac Sim 데이터 생성, (2) 류길남의 검출 모델 학습, (3) VIO 파트로의 데이터 전달을 모두 묶는 기준이 된다.
> v0.1 → v0.2: 미결 항목 전원 확정. 잔여 확인 사항은 §7 참조.

---

## 0. 문제 세팅 요약

- 창문(사각 개구부)들이 공간에 **임의 위치**로 흩뿌려져 있다.
- 통과 **순서는 색깔로 라벨링**되어 미리 정해진다.
- 이번 프로젝트의 실질 목표는 **시뮬레이션(Isaac Sim)** 구동. 하드웨어는 여유 시 확장.
- 데이터셋은 **Isaac Sim 내에서 자동 생성**(조윤호 담당)한다.

---

## 1. 이미지 처리기의 목표

카메라 프레임에서 **"다음에 통과할 창문이 지금 화면 어디에 있는가"**를 실시간으로 산출한다.

책임 범위:
1. 프레임 내 창문(사각 개구부) 검출 — **4-corner keypoint 방식 (YOLO-pose 기반)**
2. 창문 색 판정(**HSV 규칙 기반 후처리**) → 통과 순서 식별
3. 창문의 이미지상 기하 정보(네 모서리·중심) 추출

책임 밖 (VIO·융합단의 몫): 창문의 3D 위치 복원, 드론 자기 위치·자세 추정.

---

## 2. 카메라 · 좌표 규격 [확정]

| 항목 | 값 | 비고 |
|---|---|---|
| 원본 해상도 | **1280 x 720 (720p)** | 데이터 생성·좌표 기준 해상도 |
| 모델 입력 해상도 | 별도 (예: 640, 학습 시 확정) | 내부 리사이즈용. 외부 좌표와 무관 |
| **좌표 기준** | **모든 corner/center 픽셀 좌표는 원본 720p 기준** | 리사이즈 후 좌표를 밖으로 내보내지 않는다 |
| 좌표계 | 좌상단 원점 (u→오른쪽, v→아래) | 이미지 표준 |
| 컬러 포맷 | RGB | |
| intrinsics | fx, fy, cx, cy, 왜곡계수 | 데이터 생성 카메라 값을 §6에 기록, **초기 1회 공유** (매 프레임 전송 X) |

### 2.5 창문의 시각 정의 [잠정 확정 2026-08-18 — 이의 있으면 회의에서 재론]

**창문 = 색 테두리 프레임 + 뚫린 개구부** (개구부 너머로 배경이 보인다). 속이 찬 색판(pane)이 **아니다**.

| 항목 | 정의 |
|---|---|
| 프레임 | 통과 순서 색(§3.1)으로 칠한 사각 테두리. 두께는 데이터셋 랜덤화 대상 |
| 개구부 | 투명(뒤 배경 노출). 통과 대상 = 개구부 내부 |
| corner 정의 | **개구부 모서리** 4점 (§4.3 순서). 프레임 외곽이 아님 — 통과 여유 계산의 기준 |
| 렌더 표현 랜덤화 (2차 데이터셋) | 이 정의를 중심으로 프레임 두께·색 채도·개구부 반투명도(0~약간)를 흔든다. **채운 색판은 랜덤화 범위에 넣지 않는다** |

근거 (실측 2건): ① 검출 단독으로는 채움이 유리(테두리 9% vs 채움 92%, `overall_gilnam/docs/domain_gap_quantification.md`)하지만 ② 윤호 PyBullet 프로토타입에서 채운 창문은 앞 창문이 뒤 창문을 가려 색 판정이 전부 red로 무너지고 삼각측량이 창문을 융합(207mm) — 파이프라인 전체에서 채움은 불가. 실물 게이트류도 뚫린 프레임이 자연스럽다.

→ 현 1차 모델(채움 학습)은 이 정의에서 재현율 9% — **재학습(체크리스트 ⓑ) 필수**의 근거이기도 하다. 윤호의 테두리 파인튜닝은 선행 실험, 본 학습은 이 정의 + 외관 랜덤화 스펙.

---

## 3. 색(=순서) 정의 [확정]

**3색으로 시작, 가변 확장 가능하게 설계한다.**
색↔순서 매핑은 코드에 하드코딩하지 않고 **설정 파일(config)로 분리**한다. 색 추가 시 테이블만 갱신.

### 3.1 색↔순서 매핑 테이블 (초기값)

| color | order_index | HSV 판정 구간 (H, OpenCV 0~179 기준) |
|---|---|---|
| red | 0 | H ∈ [0,10] ∪ [170,179], S ≥ 100, V ≥ 80 |
| green | 1 | H ∈ [50,70], S ≥ 100, V ≥ 80 |
| blue | 2 | H ∈ [110,130], S ≥ 100, V ≥ 80 |

- 시뮬 창문 색(윤호)과 판정 코드(류길남)는 **이 테이블을 단일 기준**으로 삼는다.
- 판정 구간은 시뮬 렌더 색 확인 후 미세조정 가능 (테이블만 수정, 코드 불변).
- 채도 높은 원색 재질 사용 권장 (조명 랜덤화에 대한 강건성 확보).

---

## 4. 데이터셋 생성 규격 (Isaac Sim / 조윤호)

### 4.1 배치 랜덤화 (도메인 랜덤화)

| 항목 | 범위 | 비고 |
|---|---|---|
| 창문 개수(씬당) | 1~5개 | 한 프레임 다중 등장 허용 |
| 위치 | 카메라 전방 작업 공간 내 랜덤 | |
| 거리 | 근·중·원거리 균등 | 원거리 소형 창문 학습 |
| 각도(yaw/pitch) | 정면~비스듬(±60° 내외) | 기울어진 창문 corner 학습 |
| 조명 | 밝기·방향 랜덤 | 색 판정 강건성 |
| 색 | §3.1 테이블의 3색, 색별 균등 분포 | |

### 4.2 부분 가림/잘림 라벨 정책 [확정: A로 시작 → C로 확장, 포맷은 처음부터 C 대응]

- **1차 데이터셋 (파이프라인 검증·초기 학습용)**: 정책 A — 네 corner가 모두 화면 안에 완전히 보이는 창문만 라벨. 빠른 제작 우선.
- **2차 데이터셋 (모델 동작 확인 후)**: 정책 C — corner별 **visibility 플래그** 도입. 화면 밖/가림 corner도 추정 좌표 + visibility=0으로 라벨.
- **라벨 포맷은 처음부터 visibility 필드를 포함**한다 (1차에서는 전부 1로 채움). 2차 전환 시 포맷 변경 없음.
- 근거: 드론이 창문 통과 직전에는 corner가 화면 밖으로 나가는 게 정상 상황. A만으로는 접근 최종 구간 탐지가 학습되지 않으므로 C 확장은 필수 경로다.

### 4.3 라벨 포맷 [확정: YOLO-pose txt]

이미지당 txt 1개, 창문(객체) 1개당 1행:

```
<class> <cx> <cy> <w> <h> <u1> <v1> <vis1> <u2> <v2> <vis2> <u3> <v3> <vis3> <u4> <v4> <vis4>
```

- class: order_index (red=0, green=1, blue=2) — §3.1 테이블
- cx, cy, w, h: 바운딩 박스 (YOLO 관례에 따라 0~1 정규화)
- (u_i, v_i, vis_i): corner 좌표(정규화) + visibility (0 또는 1)
- **corner 순서 고정: 좌상 → 우상 → 우하 → 좌하** (**접근측에서 본 기준**, 시계방향 — 3D에서는 보는 쪽에 따라 방향이 뒤집히므로 "정면"을 접근측으로 명시. corner 유도 normal 부호 관례와의 연결은 `overall_gilnam/docs/state_window_interface_spec_v0_1.md` §3.1 잠정 확정 참조, 2026-08-08)
- 저장 구조: `images/{train,val,test}/*.png` + `labels/{train,val,test}/*.txt`
- 분할: train 80% / val 10% / test 10%
- 규모: 1차 3,000장 내외 → 2차 확장

### 4.4 GT 스트림 (파이프라인 검증용)

모델 학습 완료 전, 전체 파이프라인 검증에는 시뮬레이터 ground-truth corner를 §5 메시지 형식 그대로 흘려보낸다. 즉 **GT 스트림도 §5 규격을 따른다** — 모델 교체 시 하류 수정 없음.

---

## 5. VIO 전달 규격 (프레임 단위 출력) [확정]

### 5.1 메시지 구조

```json
{
  "timestamp": 1234567890123456789,   // int, ns. 카메라 프레임 취득 시각 (VIO/IMU 동기화 기준)
  "frame_id": 42,                     // int, 프레임 일련번호
  "windows": [                        // 검출된 창문 리스트 (0개 이상)
    {
      "order_index": 0,               // int. §3.1 테이블 기준 (필수)
      "color": "red",                 // string. 디버깅용 병기
      "corners": [[u1,v1],[u2,v2],[u3,v3],[u4,v4]],  // 720p 기준 픽셀. 순서: 좌상→우상→우하→좌하
      "corner_vis": [1,1,1,1],        // corner별 visibility
      "center": [u,v],                // 중심점 (편의)
      "det_conf": 0.97,               // 탐지 신뢰도 0~1
      "color_conf": 0.97              // 색 판정 신뢰도 0~1 (분리 제공 → 하류 필터링용)
    }
  ]
}
```

### 5.2 확정 사항

- **depth 미포함**: 이미지 처리기는 픽셀 좌표만 제공. 3D 복원은 VIO/융합단이 pose 변화 기반 삼각측량으로 수행. (시뮬 depth는 하드웨어 이전 시 통하지 않는 치트이므로 규격에서 배제)
- **순서 표현**: order_index(int) 필수 + color(string) 디버깅용 병기.
- **좌표 기준**: 항상 원본 720p 픽셀 좌표 (§2).
- **intrinsics**: 매 프레임 전송하지 않음. 초기 1회 공유 (§6).

---

## 6. 카메라 캘리브레이션 기록 (초기 1회 공유)

> 데이터 생성 카메라 확정 시 윤호가 기입. 이 값이 VIO 3D 복원의 기준이 된다.

| 항목 | 값 |
|---|---|
| 해상도 | 1280 x 720 |
| fx, fy | (기입) |
| cx, cy | (기입) |
| 왜곡계수 | (기입, 시뮬 카메라는 통상 무왜곡) |

### 6.1 카메라-IMU 외부 파라미터 및 pose 기준 프레임 [잠정 확정 2026-08-18 — 이의 있으면 회의에서 재론]

| 항목 | 규약 |
|---|---|
| **pose 스트림의 기준 프레임** | VIO/GT pose 메시지의 position·orientation은 **IMU(=body) 프레임**의 world 상 pose (`nav_msgs/Odometry` 관례, state_window spec §6.1). 카메라 pose가 아니다 |
| **body 프레임 축** | x 전방 / y 좌 / z 상 (PyBullet 기체 프레임 = Isaac 관례와 동일). world는 Z-up |
| **카메라 장착** | body +x를 보는 전방 카메라. 카메라 광학 프레임은 OpenCV(z 전방·x 우·y 하) |
| **T_IC (cam→IMU) 표준값** | 회전 R_IC = [[0,0,1],[−1,0,0],[0,−1,0]] (광학 z→body x, 광학 x→body −y, 광학 y→body −z), 병진 = 장착 오프셋(시뮬 기본 0). 실기 캘리브레이션(Kalibr) 시 이 표에 값만 교체 |
| **소유** | 태민 `window_recon_node.py`의 상수가 **단일 진실**. 시뮬(윤호)·GT 스트림(길남)은 이 값을 읽어 따른다 (하드코딩 복제 금지) |
| **합성 스트림(길남 sample_stream)** | 계약은 body≡camera(항등) — 이 규약과 다르므로 **태민 노드에 직접 물리지 않는다** (E2E 리허설 전용). 물리려면 T_IC를 곱해 IMU pose로 변환 후 전달 |

근거: 태민 노드에 하드코딩된 EuRoC cam0→IMU 값(z_cam 열 ≈ [0.004, 0.026, 0.9997])은 OpenCV 규약으로 읽으면 **카메라가 위를 보는 배치**가 된다. 시뮬 안에서 양끝을 같은 값으로 맞춰 자기모순은 없었지만 OpenVINS처럼 규약을 그대로 믿는 외부 도구에 넘기면 어긋남 (윤호 8/18 발견 — `prototype_demo/overrides/frames.py`; 태민도 실제로는 R_ICᵀ·R_VC로 되돌려 쓰고 있었음). 위 표준값이면 복원 경로와 EuRoC 덤프 경로가 같은 프레임으로 수렴한다.

---

## 7. 잔여 확인 사항

- [ ] Isaac Sim Replicator의 4-corner keypoint 추출 방법 확인 — 기본 출력이 아닐 수 있어 3D 모서리 점 투영 커스텀 처리 필요 가능성 (윤호, 공식 문서 확인)
- [x] §3.1 HSV 판정 구간을 실제 시뮬 렌더 색으로 검증 후 미세조정 (류길남) — **완료 (2026-08-02)**: 재생성 val 150장 정답 94.8%, 대역 유효·조정 불필요. 오판 19건은 창문 겹침 기인 → 겹침 정책은 별도 안건
- [ ] §6 intrinsics 기입 (윤호, 카메라 확정 시)
- [x] 모델 입력 해상도 확정 (류길남, 학습 시) — **완료**: 640 (vision/model_decisions.md #6, 1차 학습에 적용)
- [x] **창문-창문 가림 라벨 정책** — **잠정 확정 (2026-08-11, 규격 관리자 결정 — 이의 있으면 회의에서 재론)**: 창문 라벨은 유지하되 다른 창문에 가려진 corner는 **visibility=0**으로 마킹 (정책 C의 "화면 밖 corner"와 동일 틀로 통합 — §4.2). 배경: 미검출 31건 중 19건이 가림 라벨 채점 문제 (`overall_gilnam/docs/miss_tail_diagnosis.md`). 2차 데이터셋 생성기(윤호)에 적용 필요
- [x] **창문의 시각 정의** — **잠정 확정 (2026-08-18, §2.5)**: 색 테두리 프레임 + 뚫린 개구부, corner = 개구부 모서리. 채운 색판은 랜덤화 범위에서 제외 (8/11의 "확정 전 외관 랜덤화"는 이 정의 중심의 폭으로 축소). 근거: 윤호 PyBullet 실측 — 채움은 색 판정·삼각측량을 무너뜨림
- [x] **카메라-IMU 외부 파라미터·pose 기준 프레임** — **잠정 확정 (2026-08-18, §6.1)**: pose = IMU/body 프레임, body x전방/y좌/z상, 전방 카메라 표준 T_IC, 태민 상수가 단일 진실. 근거: EuRoC 값 하드코딩이 "위를 보는 카메라"로 읽히는 문제 (윤호 8/18)
- [ ] **검출 클래스 설계** (2026-08-18 추가, 2026-08-18 서술 정정): 단일클래스 + HSV 후처리(현 스펙 §3) vs 3클래스 직접 분류(윤호 파인튜닝). 쟁점은 **색 판정을 후처리에 둘 것인가 네트워크에 둘 것인가**다 — HSV는 겹칠 때 박스 안에 뒤 창문 픽셀이 섞여 오판 19건(실측), 3클래스는 그 실패 모드가 없는 대신 색 집합이 3개로 고정되고 hue 증강을 못 쓴다. **정정**: "같은 색 중복 시 식별 불가"는 3클래스만의 문제가 아니다 — 단일클래스+HSV도 같은 색 창문 여러 개를 구분하지 못하며(§4.1), 이는 클래스 설계가 아니라 추적·순서 문제다(윤호 `--seq`의 '직전 창문 통과 이후 샘플만 쓰는 관측 창'이 그 해법). 회의 안건

---

## 8. 역할 접점

- **류길남**: 본 규격 관리, 4-corner 검출 모델 학습·정밀화, HSV 색 판정, VIO 전달 인터페이스 구현
- **조윤호**: Isaac Sim 환경·데이터셋 자동 생성 (§2~4 준수), §6 기입
- **VIO 파트(박태민)**: §5 규격으로 corner + pose 융합, 3D 창문 위치 복원
- **박성진**: PID 저수준 제어 (하류 연결)

---

*v0.2 — 2026-07-02 확정. 변경 시 이 문서를 갱신하고 팀에 공유할 것.*
